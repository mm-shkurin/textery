[pytest]
asyncio_mode = auto
markers =
    backend: Backend API acceptance tests (HTTP, black-box, no compile dependency on backend internals)
    frontend: Frontend UI acceptance tests (Selenium/Playwright)
