# Task 5: Toolbar actions don't mark the document dirty, so the save status falsely reads "Сохранено" — Progress

Type: bug

## Spec
- [x] spec

## Fix: dirty flag only set by the DOM input handler, never by programmatic transactions
- [x] root cause analysis — confirmed by probe during Story 5 scenario 7.9's premortem: `setHasUnsavedChanges(true)` has one call site, `editorProps.handleDOMEvents.input` (`ManualEditor.tsx:117-118`); programmatic toolbar transactions emit no DOM `input` event. See spec.md for the probe output and the `hasUnsavedChanges` init-to-`true` trigger detail.
- [S] design — fix approach is obvious and single-option: move the flag to Tiptap's `onUpdate`, which fires on any document-changing transaction regardless of origin. No competing approach worth an ADR; the one non-obvious seam (`setContent` on reopen must NOT mark dirty) is a test-scoping detail, recorded in spec.md and pinned in the red phase, not a design decision. **Post-scan note:** the `[S]` still holds — `onUpdate` remains the only approach — but the scan proved the *scope* was mis-stated, not the approach. See spec.md "Fix approach" for the three corrections.
- [x] steps discovery (scope: frontend component `ManualEditor.tsx` + hook `useDocumentInit.ts`, no API/CSS/backend change; scanned all _index.md groups 01-08, none skipped, 06 CLEAR; GAPs: 4 folded as red-* — in-flight toolbar twin [02/03/04/05/07/08 converged], load direction + init-`true` masking trap [04/08], first-edit-after-load fail-open [05], failed-save flag pin [07]; dismissed: exit guard `beforeunload` → separate change, belongs with carryover.md's open BLOCK; re-save idempotency → server property, backend unreachable; `console.error` attribution → pre-existing, fix touches neither catch nor error contract; double-click-Save exactly-once → pre-existing, 4.2 covers the input path; multibyte fixture → moot once discriminator is origin-based)
- [x] red-frontend — new file `ManualEditor.dirty.test.tsx` (104 lines after test-review). **Test 1** (`it.skip`, genuinely red): drive content → save → observe `Сохранено` → select → click `toolbar-bold` → expect `Черновик, ещё не сохранён`. **Predicted failure:** jest-dom AssertionError at the status assertion — `received value must be an HTMLElement or an SVGElement. Received has value: null` — because `setHasUnsavedChanges(true)`'s sole call site is the `handleDOMEvents.input` handler and a programmatic toolbar transaction emits no DOM `input` event. **Actual failure:** matched exactly (type/message/status all YES); the preceding content assertion `innerHTML === '<strong>hello</strong> world'` passed, and the observed-clean gate passed, so the document genuinely diverged — not a stored-marks false positive, not an init-`true` artifact. **Test 2** (characterization, no marker): rejected `saveDocument` leaves the status dirty. **Predicted: PASSES** (the catch never touches `hasUnsavedChanges`). **Actual: passed first run. Comparison: match.** Pins 07's GAP 2 against a future symmetry mistake in the catch.

  **test-review strengthened it four ways, all real:** (1) the clean gate proved the *flag* but never *what was saved* — added `toHaveBeenCalledWith('doc-1', 'hello world', 1)`, which is what makes the divergence an assertion rather than an inference; (2) test 2 could pass without a save happening at all (an early `!documentId` return leaves the doc trivially dirty) — added exact-args + `toHaveBeenCalledTimes(1)` + an assertion that `console.error` received the exact rejection object, proving the catch branch ran; (3) `consoleErrorSpy.mockRestore()` sat in the test body, so any throw before it left `console.error` stubbed for the rest of the file — `clearAllMocks()` does not restore spies; moved to `vi.restoreAllMocks()` in `afterEach`. The same leak exists at `ManualEditor.saveStatus.test.tsx:77,88,107,122` — pre-existing, not touched here; (4) `toHaveBeenCalledTimes(1)` after the toolbar click, so `Черновик` can only come from the flag and not an incidental re-save. Verified the red still fails for the predicted reason after all four. **Note:** the failure *message* is now testing-library's `Unable to find an element with the text` (the `queryByText`→`getByText` switch), not the jest-dom text quoted in the prediction above — same assertion, same code path, better message. Confirmed `it.skip` is the sanctioned frontend disable marker (`frontend-rules.md:37`), not a rule violation. Suite: 47 passed, 1 skipped (the intended red), 0 failed.
- [ ] red-frontend-inflight — the twin five groups converged on: a **toolbar** action during an in-flight save must queue a re-save. Assert a second `saveDocument` fires carrying the marked-up HTML at the server-returned version, and the status never reads `Сохранено` while unsent content exists. `ManualEditor.saveStatus.test.tsx:125` is the right shape but drives `fireEvent.input`, so it stays green through this regression.
- [ ] red-frontend-load — reopen via `existingDocumentId` renders `Сохранено` (not `Черновик`) and fires no `saveDocument`; a toolbar edit immediately after the load still marks dirty (05's fail-open: an over-broad suppression would swallow the first edit after every document open, and both other directions would still pass).
- [ ] green-frontend — single fix resolving all three red surfaces: both writes move to `onUpdate` behind one user-edit discriminator; `setContent` suppressed via `emitUpdate: false`; load path actively sets clean.
- [S] align-design — no visual change: the status line's three states and their styling already exist and are aligned (scenario 5.1's align-design); this fix only corrects *when* each state is shown.
- [S] red-acceptance / green-acceptance — the fix changes externally observable behavior (the status line), which normally forces an acceptance pair. Backend is on a parallel branch and every Selenium step in Story 5 is `[S]` under that story's standing policy, so no acceptance-level guard can run here. Owed once a live backend reaches this branch. See spec.md "Acceptance scope".
- [S] demo — same reason: no live backend to drive a visible Selenium run against.
