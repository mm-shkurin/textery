# Story 18: Generate → edit — Frontend Progress

Owns: Frontend Scenarios. Narrative/decisions/Spec checklist live in `progress.md`;
`ProductSpecification/stories.md` is the cross-file rollup.

This file tracks **which work units ran**.

## Frontend Scenarios (02_UI_Tests.md)

### Scenario 1.1: Selecting a type goes straight to generation
- [x] red-selenium — analytical RED (skipped; live run deferred to green-selenium). New `test_generate_flow_acceptance.py` (35) + `generate_flow_statements.py` (37) + conftest fixture. Asserts pick-doklad-type → `[data-testid='generation-generating']` visible AND `MODE_MODAL` NOT shown. Predicted RED: TimeoutException on `generation-generating` — current `useFlowNavigation.selectType` sets `step='mode'` (mode modal), no generating surface renders. New contract testid `generation-generating` (also 1.2's subject). test-review PASS 0 fixes. **Owed for green-selenium (non-gating, agent-review+premortem converge): (1) transient-surface flake — `assert_generation_started` waits on `generation-generating` with `live_session=True`; a fast real backend can transition past the generating state between polls → spurious TimeoutException indistinguishable from regression. green-selenium must pin generation to a deterministic pending window (slow/stubbed poll) before un-skip. (2) `assert_no_mode_modal_shown` (invisibility_of MODE_MODAL) passes vacuously for ANY modal-absent state (landing/error/editor) — only earns its keep because `assert_generation_started` runs first; coupled to (1). Consider adding a positive assertion that exactly one POST /generations fired (base DSL has `_count_requests_to`). Nit: redundant `element.is_displayed()` after `_wait_for_visible`.**
- [x] red-frontend — live vitest RED on `useFlowNavigation` hook seam. Added `it.skip` test: `selectType('doklad')` → `step==='form'` + `mode==='auto'` (straight to generation, no `'mode'` step). RED confirmed: `AssertionError: expected 'mode' to be 'form'` (useFlowNavigation.test.tsx:78) — current `selectType` sets `step='mode'`, `mode=null`. Matched prediction. test-review +1 fix: added `documentType==='doklad'` + `openDocumentId===null` (openDocumentId null is what makes ChatWorkspace POST a NEW generation vs GET existing). Suite 12 pass / 1 skip. **green note:** flipping `selectType` to `setStep('form')+setMode('auto')` breaks the existing "walks type then mode" test + makes `selectMode`/`backToModeModal`/`'mode'` step dead for the create path — handle in green/refactor. **Owed for green (agent-review+premortem, converge): (A, IMPORTANT) contract testid `[data-testid='generation-generating']` (used by red-selenium 1.1) does NOT exist — `ChatWorkspace.tsx` exposes `chat-panel`/`doc-area`, and `GenerationUiState` is `idle|pending|completed|failed` (no `generating`). green MUST reconcile: either add a `generation-generating` testid on the pending/generating surface, or repoint the Selenium locator to an existing testid + generation `pending` state. Also: at `step='form'&mode='auto'` the workspace renders `idle` unless `selectType` also triggers generation — pin that `form+auto` actually kicks off generation (renderer-level or Selenium), not just hook state. (B) `backFromEditor` for a new (non-history) doc falls through to `backToModeModal()`→`step='mode'` (now forward-unreachable but Back still routes there → orphaned modal); green must repoint Back + remove/retire dead `selectMode`/`backToModeModal`/`'mode'` wiring. Sibling tests `walks type then mode`, `returns a newly created document to the mode modal`, `unwinds the whole flow on sign-out` will break — green must preserve the sign-out poll-stop guarantee (add a single-step `selectType`-only logout-unwind test). (C) `mode` collapses to constant `'auto'` on create path — decide if generating-vs-editor should key off `openDocumentId` alone.**
- [x] green-frontend — `selectType` now sets `step='form'`+`mode='auto'` (straight to generation). Removed `selectMode`/`backToModeModal`/`'mode'` step; DELETED `ModeModal.tsx`/`.css`/test (dead). FlowLanding step narrowed to `landing|type`. `backFromEditor` (new/non-history doc) → `generation.reset()`+`step='type'` (history path unchanged → `history`). Added `data-testid='generation-generating'` on DocArea **pending** branch (contract A resolved by adding testid, not repointing locator). Siblings updated w/o weakening: removed `walks type then mode`; `...to mode modal`→`...to type step`; sign-out unwind converted to single-step `selectType`-only, poll-stop preserved. App.test.tsx integration updated (dropped 2 mode-modal→manual-editor entry tests; ManualEditor covered by own suite + history path). Suite **526 pass / 0 fail**, tsc clean. **CAVEAT (blocks green-selenium 1.1):** did NOT auto-submit generation at `form+auto` — `createGeneration` needs a real topic, none exists until composer used; workspace opens on idle composer, so `generation-generating` (pending) is not yet reachable on load. Auto-start + deterministic pending window owed to red-frontend-api/align-design BEFORE green-selenium 1.1 un-skips (remove-marker-only must not run prematurely). **Review passes on 916ab0a (refactor NO-OP; agent-review CONCERNS/4 + premortem CONCERNS/2):** (1) FIXED IN THIS UNIT — commit broke Story 5 `test_mode_modal_acceptance.py` (waited on removed `MODE_MODAL`); marked `@pytest.mark.skip` OBSOLETE (mode modal removed by Story 18). `ModeModalStatements` fixture + `MODE_CARD_AUTO` base locator now unused (harmless; retire in a later cleanup). (2) OWED — **manual-create path now unreachable**: `mode='manual' && openDocumentId===null` is dead (only `openDocumentFromHistory` sets manual, always with an id), so Story 5's blank/non-AI editor create is gone. INTENDED by Story 18 (unify), to be restored by **scenario 6.1** "blank document from scratch" — but nothing marks it intentionally-down; if story ships before 6.1 it's a silent regression. 6.1 MUST add an integration guard that a signed-in user reaches a blank ManualEditor. (3) OWED — vacuous hook test: `useFlowNavigation` 1.1 test named "goes straight to generation" only asserts hook state; the generating surface/POST is not proven at renderer level (composer idle on load). Covered by the green-selenium block caveat + owed auto-start. (4) MINOR — dead `backToTypeModal` hook export (only its own test references it) + unreachable `backFromEditor` new-doc else-branch (→`type`); trim when 6.1 wires create-path editor. (5) REMOTE — ChatWorkspace has no back-to-type affordance; mis-picked type only escapable via sign-out (pre-existing, low-sev).
- [x] red-frontend-api — live vitest RED on the `generationApi` wire contract. New `generationApi.documentType.test.ts` (`it.skip`): `createGeneration(topic, 'referat')` must send `document_type: 'реферат'`; today `createGeneration` takes only a topic and hardcodes `WIRE_DOCUMENT_TYPE[DEFAULT_DOCUMENT_TYPE]`, so whichever type card is pressed the wire says `доклад`. RED confirmed: `AssertionError: expected 'доклад' to be 'реферат'` — matched prediction verbatim (type/message/expected/received/location/1-failed all matched). **Subject choice:** red-agent deliberately did NOT write the "picking a type fires exactly one auto-POST" test the green-frontend caveat asked for — the pending-state half is already pinned by `useGeneration.test.ts:18`, and the auto-POST half is hook/renderer wiring, not `generationApi`. More importantly its premise is contested: `createGeneration` needs a real topic, none exists until the composer is used, an empty-topic POST is a backend 422 — so a RED demanding auto-POST would force a wrong implementation. **The topic-source question (where does a topic come from before the first POST?) is a product decision owed to `align-design`; green-selenium 1.1 stays blocked.** test-review +6 fixes applied: full-body `toEqual` (caught unasserted `volume_pages`), exact URL `/api/v1/generations` + method `POST` + exact header set, `Idempotency-Key` pinned via `vi.stubGlobal('crypto', {randomUUID})`, return-value `toEqual({generationId,status})` (mapping regression was invisible), `toHaveBeenCalledTimes(1)` (duplicate POST passed before). File 65L. Suite `src/features/generation/api/__tests__`: 23 pass / 0 fail / 1 skip. **Owed to green-frontend-api:** widen to `createGeneration(topic, documentType)` mapping via `WIRE_DOCUMENT_TYPE`; thread the type through `useGeneration.submit` (`useGeneration.ts:130`, topic-only today) from `ChatWorkspace`/`DocumentGenerationFlow` via `flow.documentType` (already in hook state). **Review passes on 6b4af28 (refactor NO-OP; agent-review CONCERNS/4; premortem BLOCK/2):** (1) FIXED IN THIS UNIT — the commit turned the branch tip un-buildable: `it.skip` suppresses execution but not compilation, `tsconfig.app.json` includes `src`, so `npm run typecheck` and `npm run build` (`tsc -b && vite build`, both in `.github/workflows/frontend-ci.yml`) failed with `TS2554: Expected 1 arguments, but got 2`. Note a bare `npx tsc --noEmit` reports clean — `tsconfig.json` is `"files": []` + project references, so only the `-b` form checks anything; **the red-phase gate must run `npm run typecheck`, not bare tsc**. Fixed with a `@ts-expect-error` on the call line: keeps CI green while skipped and is self-removing (an unused `@ts-expect-error` is itself an error once green widens the signature). Verified: typecheck clean, suite 23 pass / 1 skip. (2) OWED, corrects the line above — `generationApi.test.ts` never passes a type: lines 29/51/71/83/96 all call `createGeneration(topic)` with ONE argument. Those five sibling call sites plus the sole production caller `useGeneration.ts:130` keep compiling only if green makes the second parameter **optional with a `DEFAULT_DOCUMENT_TYPE` default**. That is a design decision green must make deliberately, not a satisfied precondition — a required parameter breaks all six at once. (3) OWED, IMPORTANT — the optional-default shape means **green can satisfy this RED, satisfy typecheck, keep all 526 tests green, and still never thread `flow.documentType` through**, leaving the wire hardcoded to `доклад`. This unit pins the contract one layer below where the defect lives. A caller-level test is required: drive `useGeneration.submit` / `ChatWorkspace` after `selectType('referat')` and assert the `createGeneration` spy received `'referat'`. (4) OWED, planning gap — the auto-start guard the green-frontend caveat owes to "red-frontend-api/align-design" now has no step left to land in: remaining steps are `green-frontend-api`, `align-design`, `green-selenium`, `demo`, and none is a red step (`green-selenium` only un-skips, `align-design` is a product decision, `demo` is observation). Either insert a new red step after `align-design`, or green-selenium 1.1's recorded block resolves by accident when someone un-skips. (5) MINOR — this unit's commit message says "whichever type card the user presses the wire always says доклад", but `doklad` is the only entry with `available: true` in `shared/documentTypes.ts`, so the defect is latent, not user-reachable today. The test file's own header comment states this correctly; the commit message (the project's only review surface) overstates it.
- [x] green-frontend-api — `createGeneration(topic, documentType = DEFAULT_DOCUMENT_TYPE)` now maps via `WIRE_DOCUMENT_TYPE[documentType]`; `useGeneration.submit(topic, documentType?)` passes it straight through; the join lands in `useFlowNavigation.submitGeneration(topic)` = `generation.submit(topic, documentType ?? DEFAULT_DOCUMENT_TYPE)`, wired as `onSubmit={flow.submitGeneration}` in `DocumentGenerationFlow`. `ChatWorkspace`'s `(topic: string) => void` prop shape is untouched — the composer still only knows the topic, the type is joined in flow state where it lives. Removed the `it.skip` + the paired `@ts-expect-error` (self-removing as designed — leaving it was `TS2578`); no assertion touched. **Signature-shape decision — optional-with-default, AGAINST the preference red-frontend-api recorded:** a required parameter is a hard `TS2554` in nine one-argument call sites that all live in *test files* (`generationApi.test.ts` 29/51/71/83/96 + 4 `submit('тема')` calls across `useGeneration.test.ts`/`.resilience`/`.slowPoll`), which `tsc -b` compiles and which a green phase may not edit. The premortem's hazard is real but its proposed fix collides with tests-read-only, so it was closed the OTHER way the premortem named — the caller-level guard. Rationale recorded in a comment on `createGeneration`. **Caller-level guard (obligation 4, the point of the unit):** new `frontend/src/app/__tests__/useFlowNavigation.documentType.test.tsx` — `selectType('referat')` → `submitGeneration('Тема')` → asserts the `createGeneration` spy got `('Тема', 'referat')` exactly once. Uses `'referat'` deliberately, not `'doklad'`: with the default in place a `'doklad'` assertion would pass against the very hardcode it exists to rule out. Verified it bites — reverting `submitGeneration` to `generation.submit(topic)` fails it while every other test stays green (exactly the defect-survives-green scenario). Sits at the hook seam rather than click-driven because `referat` is `available: false` and not clickable in `TypeModal`. Suite **528 pass / 0 fail / 0 skip** (124 files; baseline 526 + un-skipped RED + new guard); `npm run typecheck` clean (run in the `-b` form per the recorded trap). All touched files under 200L (147/94/161/70). **Still open, NOT fixed here (out of green scope):** the auto-start caveat — `submitGeneration` only fires when the composer is used, so the `generation-generating` pending surface is still unreachable on load and **green-selenium 1.1 remains blocked**. This unit threads the type; it does not create a topic source. Owed to `align-design`, and there is still no red step after it to land an auto-start guard in. **Review passes on 21dff5a (refactor applied 1; agent-review CONCERNS/3; premortem CONCERNS/1 — both converge on the same gap):** (1) FIXED IN THIS UNIT — the guard stopped one line short of the defect. `DocumentGenerationFlow.tsx:104` (`onSubmit={flow.submitGeneration}`) is the ONLY production edge joining the thread-through to the UI, and nothing tested it: `useFlowNavigation.documentType.test.tsx` calls `submitGeneration` directly on the hook, so it never sees which callback the component hands down. Because both new parameters are optional-with-default, reverting that line to `flow.generation.submit` is NOT a type error (an optional trailing parameter stays structurally assignable to `ChatWorkspace`'s `onSubmit: (topic: string) => void`) and NOT a test failure — every card silently generates a `доклад` again with all 528 green. Verbatim the hazard the unit claimed to close. Closed with `frontend/src/app/__tests__/DocumentGenerationFlow.documentType.test.tsx` (53L): renders `App` with `generationApi`/`documentApi` mocked and drives the real UI (CTA → `type-card-doklad` → `topic-input` → `topic-send`), asserting `createGeneration` called exactly once with `(TOPIC, 'doklad')`. **Bite verified:** with line 104 reverted the test FAILS (`- "doklad"` / `+ undefined` as the 2nd arg); restored, it passes. (2) FIXED IN THIS UNIT — the `openDocumentFromHistory` comment claimed `documentType`'s `?? 'doklad'` fallback is "display text" for the breadcrumb LABEL only. False since this unit: `submitGeneration` reads the same field onto a POST. Not reachable today (the history path sets `mode='manual'` + `openDocumentId` → `ManualEditor`, and every route back to create passes through `selectType`), but the invariant was wrong in the file that has to know it. Comment corrected to state the new constraint for any future path into `step='form'`. (3) OWED, minor — `submitGeneration`'s `documentType ?? DEFAULT_DOCUMENT_TYPE` is a silent coercion. The branch is genuinely unreachable (`DocumentGenerationFlow.tsx:65` gates the workspace on `step === 'form' && documentType && mode`), but its stated purpose ("the composer cannot post a typeless request") is achieved by posting a *wrong-typed* request — the exact failure mode this unit exists to eliminate. If truly unreachable, not submitting (or throwing) fails loudly instead of billing a `доклад` nobody picked. No guard for either behaviour. Suite after all fixes: **529 pass / 0 fail / 0 skip** (125 files); typecheck clean.
- [x] align-design — **DECISION (user, 2026-07-28), resolves the block three units carried:** scenario 1.1's "picking a type goes straight to generation" means *the mode-select modal is gone* — the user lands directly on the topic composer and generation starts on send. It does NOT mean a POST fires at type-pick time. Story 01's own mockup `04-generation-form.html` always put topic entry AFTER the type pick (breadcrumb type chip → mode chip → "Новая генерация / Опишите, что нужно получить" → topic field), and story 18 removed only the modal, so today's `type → Composer → send` is faithful to that flow. **Consequence: the owed "auto-start generation" work is CANCELLED, not deferred — there is no topic source to design because the composer is the topic source.** `red-selenium` 1.1 must be relaxed before `green-selenium` can un-skip: it currently waits on `generation-generating` immediately after the type pick, which under this reading is wrong — it should assert the composer surface + no mode modal, and (per the earlier red-selenium note) that exactly one `POST /generations` fires on send. **DECISION 2 (user):** align against story 01's mockups rather than skipping — story 18 has no mockups of its own (Spec `[S]`), and the surfaces are story 01/05's. Drift found and fixed: the standalone form page was skipped by story 01 (known-debt #9) so its identity elements never landed anywhere — the idle screen had **zero** confirmation of which type was picked. Added `GenerationHeading.tsx`/`.css` (mockup 04 breadcrumb + title + subtitle, rendered only when `state === 'idle'`; the status badge owns the slot once a generation exists). Extracted `Composer.css` out of `ChatWorkspaceDoc.css` (which sat exactly at the 200-line cap → now 169) and corrected composer tokens to mockup 04 (padding `14px 16px` not `12px`, font `15px` not `14px`, focus border `--border-strong`, `::placeholder` color, submit row `14px 32px`/`15px`), added the required-marker and the `Обычно занимает 1–2 минуты` hint (which previously appeared only on the *pending* screen — the user was told the wait only after committing to it). **Breadcrumb decision:** dropped the mockup's *mode* chip, kept only the type chip — story 18 deleted the mode modal, so that chip could only render a hardcoded `Автоматический режим`, decorating a choice the user is never offered. Also NOT ported: mockup 04's `← Назад` (ChatWorkspace takes no back callback; wiring one is a behavior change, not alignment). Container stays at mockup 05's `1100px`/`40px` rather than 04's `1200px`/`48px` so the page does not jump width on submit. **/design-review FAILED first pass** — `Composer.tsx:22` hardcoded `Тема доклада` lifted from mockup 04, sitting five lines under a breadcrumb rendering the real picked type, so any non-доклад type showed two different type names on one screen; `aria-labelledby` made it the textarea's accessible name too, so a screen reader announced the wrong one. Fixed: new hand-written exhaustive `DOCUMENT_TYPE_GENITIVE` + `topicFieldLabel(documentType)` in `shared/documentTypes.ts` (genitive cannot be derived from `name` — доклад→доклада, эссе→эссе, сочинение→сочинения), `Composer` takes a `topicLabel` prop, `ChatWorkspace` takes `documentType` alongside `documentTypeLabel` (the shape `ManualEditor` already uses). New test asserts a `referat` render is named `Тема реферата`. **/design-review re-run PASS** — full mockup-04 text inventory re-diffed, zero surviving literals. Suite **530 pass / 0 fail / 0 skip**; typecheck clean; lint clean. Coverage on the focused set: `GenerationHeading` / `Composer` / `documentTypes` all 100%; one real gap recorded below. **OWED (non-gating, from /design-review):** `Composer.tsx` applies `cw-btn cw-btn-primary` but imports only `Composer.css` — those rules live in `ChatWorkspaceDoc.css` and it renders today only because `ChatWorkspace` happens to import that sheet. Invisible dependency; jsdom applies no CSS so no test catches it. Fix is NOT moving `cw-btn` into `Composer.css` (`DocArea` uses it too) but a small shared button stylesheet imported by each consumer. **OWED (repo-wide, pre-existing):** `lucide-react` is not installed despite the react-ts binding naming it, so all 6 icon sites render the generic `PlaceholderImage` square instead of real glyphs. **Review passes on 3dde12d (refactor applied 2; agent-review CONCERNS/4; premortem CONCERNS/3):** (1) FIXED — **live breakage the earlier fallout list missed.** `chat_workspace_statements.navigate_to_chat_workspace_for_doklad` still ended with a `MODE_CARD_AUTO` click; the mode modal was deleted in 916ab0a, and `test_chat_workspace_acceptance.py` is NOT skipped — so that whole suite has been dead (every test timing out on a removed element) and nobody noticed because 1.1 was expected red. Click dropped; helper now ends at the type pick. Retired the genuinely-dead remainder: deleted the obsolete `test_mode_modal_acceptance.py`, the `ModeModalStatements` class + its conftest fixture, and `MODE_CARD_AUTO`. `mode_modal_statements.py` survives as locators only — `MODE_MODAL` and `MODE_CARD_MANUAL` still have live users (`assert_no_mode_modal_shown`, `assert_mode_modal_is_closed`). Acceptance collects **40 tests / 0 errors**. (2) FIXED — the unit's headline change was unguarded: deleting the `<GenerationHeading>` branch left all 530 green. Added ChatWorkspace cases — idle renders `generation-breadcrumb` + `Новая генерация` and NO badge; `pending` renders `В обработке` and neither breadcrumb nor title. (3) FIXED — the two-type bug was re-openable from the caller: `documentType` and `documentTypeLabel` are independent props, and nothing enforced the pairing at the component boundary. Added a test rendering `documentType="referat"`/`documentTypeLabel="Реферат"` asserting the breadcrumb AND the textarea's accessible name name the same type. Kept as a test rather than deriving the label internally — `ManualEditor` takes the same pair across ~20 call sites. (4) FIXED — `.composer h3.required::after { content: ' *' }` sat on the textarea's `aria-labelledby` target, and real browsers fold generated content into the accessible name, so the true name was `Тема доклада *` while the unit tests assert `Тема доклада`; jsdom applies no CSS so no test could ever see it. Marker moved into markup as an `aria-hidden="true"` span, `::after` dropped, guard test added. Mattered imminently — red-selenium 1.1 targets this screen. (5) OWED, MINOR — the commit message called the `Обычно занимает 1–2 минуты` hint a *move*; it is a duplication. `DocArea.tsx:61` still renders its own longer variant, so the wait-time literal now exists twice and must be kept in sync by hand. (6) OWED, MINOR — the page loses its only `<h1>` the moment a generation starts: `GenerationHeading` is the sole top-level heading and is mounted only while idle, so from first submit the surface is `<h3>`-and-below. Document-outline regression introduced by the slot swap. (7) OWED, **product question, blocks nothing here but is the same class as (1)** — `manual_editor_statements.open_manual_editor_for_doklad` still clicks `MODE_CARD_MANUAL`, and `test_manual_editor_acceptance.py` plus ~10 sibling manual-editor tests are NOT skipped, so that suite is dead too. Unlike (1) it is not a mechanical delete: with the mode modal gone manual mode has no reachable entry at all. Belongs with **scenario 6.1** ("blank document from scratch"), which already owns restoring that path. Suite after fixes: **534 pass / 0 fail / 0 skip**; typecheck clean.
- [x] red-frontend (coverage: blank topic on Ctrl+Enter does not submit) — `it.skip` case in `ChatWorkspace.test.tsx`: types `'   '`, fires Ctrl+Enter and Cmd+Enter, asserts `onSubmit` never called. Seam is `ChatWorkspace` (via the existing `renderWorkspace()` helper), not `Composer` — the guard lives in `ChatWorkspace.send()` while the unguarded `onKeyDown` lives in `Composer`, so only the workspace-level render exercises the real path end to end. **The guard already exists, so this could not be a conventional RED** — bite verified instead per tdd-rules: with `if (trimmed)` removed the test FAILS (`expected "vi.fn()" to not be called at all, but actually been called 1 times`, received arg `[""]`), restored it passes 8/8, and `git diff` on `ChatWorkspace.tsx` is empty. **Prediction missed once and was corrected rather than waved through:** predicted the spy label `"spy"`, actual was `"vi.fn()"` (vitest names an un-named `vi.fn()` after itself) — re-predicted, re-ran unchanged, all fields matched on run 2. test-review +7 fixes: (A5, the important one) the test was **negative-only** — both `not.toHaveBeenCalled()` arms also hold if `Composer`'s `onKeyDown` is deleted outright, pinning nothing about `if (trimmed)`; added a control arm retyping `'  Тема  '` + Ctrl+Enter asserting `toHaveBeenCalledTimes(1)` and `toHaveBeenCalledWith('Тема')`. (A6) `onSubmit.mockClear()` between the ctrl and meta dispatches — the meta assertion was re-reading the ctrl arm's state, so a meta-key regression was unobservable. Plus 5 loose-substring assertions anchored across the pre-existing cases (`/7 страниц/` → the exact full meta line; `doc-body` → `/^Готовый текст$/`; breadcrumb chips → `/^Доклад$/`, `/^Реферат$/`; `doc-error` scoped with `within` + exact heading) and a missing `toHaveBeenCalledTimes(1)` on `calls onSubmit with trimmed topic`. Suite **534 pass / 0 fail / 1 skip**; typecheck clean. **Note:** the recorded-failure comment cites `ChatWorkspace.test.tsx:90:26`; test-review's edits shifted that assertion to line 96 and the comment was preserved verbatim per the marker rule, so the line reference is stale — refresh it when green removes the marker. **Review passes on 0c7814c (refactor NO-OP; agent-review CONCERNS/3; premortem CONCERNS/3):** (1) FIXED IN THIS UNIT (premortem, credible) — **double-submit bills twice.** `Composer`'s `onKeyDown` has no in-flight guard and the send button is disabled only on a blank topic, never during a request; `ChatWorkspace.send()` is not re-entrant-safe either. `createGeneration` mints a fresh `crypto.randomUUID()` per call and that `Idempotency-Key` is scoped to the internal 401-replay, so two user-initiated submits carry two different keys and the backend cannot collapse them — the first generation then runs to completion unwatched because the client polls the second. What actually prevents it today is **render ordering, not a guard**: `useGeneration.submit` sets `pending` synchronously, swapping `Composer` for `Progress` before the next event is delivered. Moving `setState('pending')` behind the awaited `createGeneration`, or keeping `Composer` mounted during pending, reopens it silently. Added a double-Ctrl+Enter test at the `DocumentGenerationFlow` seam asserting `createGeneration` is called exactly once. **Honest caveat: this is a characterization/regression pin, not a bite-verified RED** — it passes against current code and would catch the regression, but no production guard was added, so the protection remains emergent. A real in-flight guard (disable submit while pending) is still owed. (2) OWED, IMPORTANT (agent-review) — **`DocArea.tsx` hardcodes `доклад` in four strings**: `:36` `Создать новый доклад`, `:46` `Не удалось сгенерировать доклад`, `:61` `Готовим ваш доклад`, `:68` `Опишите тему доклада` — while `:29` correctly uses the `label` prop. Verbatim the defect class `/design-review` failed `Composer.tsx:22` for one unit earlier: a `referat` run would show `Реферат` in the breadcrumb and `доклад` in the error panel, two type names on one screen. Latent only because `doklad` is the sole `available: true` type. **Aggravated by this commit**: tightening the error assertion to `/^Не удалось сгенерировать доклад$/` promoted the hardcoded literal into an asserted contract, so whoever threads `label` through must now change a passing test to do the right thing. No test renders `DocArea` in `completed`/`failed`/`pending` with a non-`doklad` label. Not fixed here because this is a RED phase — production copy changes are out of bounds. (3) OWED, MINOR (agent-review) — the newly exact meta-line assertion pins `7 страниц`, the one arity where a missing Russian pluralization rule is invisible: `DocArea.tsx:29` renders `страниц` unconditionally, so `1 страниц` / `2 страниц` / `22 страниц` are all ungrammatical, and the `{volumePages ?? '—'}` fallback renders `— страниц`. (4) OWED, MINOR (agent-review) — the anchored `создан только что` segment is inert: `renderWorkspace` never passes `createdAt`, so `formatRelativeTime(null)` returns that string and the assertion holds identically whether the `createdAt` prop-drill works or is severed. (5) OWED (premortem) — the empty-topic invariant is enforced only at the outermost UI leaf. `submitGeneration` → `useGeneration.submit` → `createGeneration` all accept blank unconditionally, so the guard holds for exactly one caller. **Scenario 6.1's restored manual-mode entry is a scheduled second caller** — a hook-level test that `submitGeneration('   ')` refuses would move the invariant to the layer that owns the wire. (6) OWED, MINOR (premortem) — the RED pins *total silence* as intended behavior for a blank Ctrl+Enter. A keyboard user is by definition not watching the greyed-out button that carries the only affordance; one positive assertion (validation message, retained focus, or `aria-live`) would make "nothing happened" a decision rather than a gap. Suite after the added guard: **535 pass / 0 fail / 1 skip**; typecheck clean.
- [x] green-frontend (coverage: blank topic on Ctrl+Enter does not submit) — marker-removal only, **zero production code changed**, as designed: the guard `if (trimmed) onSubmit(trimmed)` already existed and was already correct; the RED was a coverage gap, not a missing behavior. Un-skipped the test and refreshed its recorded-failure comment — the verbatim RED comment cited `ChatWorkspace.test.tsx:90:26`, which `/test-review`'s edits had shifted; replaced with a positional reference ("the first `not.toHaveBeenCalled()` below") that cannot rot again, and dropped the now-past-tense "RED," prefix and the stale "green removes this marker only" line. The substance is intact: disabled button vs unguarded `onKeyDown`, `if (trimmed)` as the only thing standing between a whitespace topic and a backend 422, plus the delete-the-guard verification and its exact AssertionError. No assertion touched — the control arm, the `mockClear()` between the ctrl and meta dispatches, and all five anchored substrings survive. Suite **536 pass / 0 fail / 0 skip** (baseline 535 pass / 1 skip — exactly the expected +1/−1); typecheck clean. The `Not implemented: navigation to another Document` lines in the run tail are pre-existing jsdom stderr noise from `App.rootErrorBoundary.test.tsx`, not failures.
  - **Gap (CLOSED by the green step above — the text below is the original finding, kept for the reasoning):** `ChatWorkspace.tsx:46` `if (trimmed) onSubmit(trimmed)` — false branch uncovered (file is 7/8 branches, 100% stmts/lines/fns). Reachable, not dead: the send *button* is `disabled={!topic.trim()}`, but `Composer`'s textarea `onKeyDown` fires `onSend` on Ctrl/Cmd+Enter with **no** disabled guard, so a whitespace-only topic reaches `send()` and must be swallowed. Untested today — deleting the `if` keeps all 530 green while letting an empty-topic POST through (backend 422).
  - **Review passes on 717e0fb (refactor applied 1; agent-review CONCERNS/1 + note; premortem CONCERNS/3):** (1) FIXED IN THIS UNIT (premortem, credible, **billing**) — **reset came back to a pre-filled composer.** `topic` lives in `ChatWorkspace`'s own `useState`, which `useGeneration.reset()` cannot reach (it clears state/content/volumePages/createdAt/error), and the workspace is never unmounted across a reset — only its inner branches swap. So "Создать новый доклад" returned to the composer still holding the topic just generated, send button already enabled: one keystroke re-bills the user for the document they already have. Unlike the double-Ctrl+Enter case this second submit is a **separate gesture**, so the emergent render-ordering protection does nothing at all. `ChatWorkspace` now clears the topic in its own `reset` before delegating. **Bite verified:** reverting `onReset={reset}` to `onReset={onReset}` fails the new test (1 failed / 10 passed); restored, 11 pass. (2) FIXED IN THIS UNIT (premortem, credible) — **both reset buttons were untested and unlocatable.** No `data-testid` on either, and nothing asserted they invoke `onReset`; severing the `onClick`, or the `onReset` prop-drill above it, stayed green across all 536 — the same shape as the `DocumentGenerationFlow.tsx:104` defect this story already caught for `onSubmit`. The failed panel is the one screen with no other exit. Added `data-testid="doc-reset"` / `"error-reset"` plus a click-asserts-`onReset` case for each (also unblocks the Selenium layer, which had no locator for either). (3) FIXED IN THIS UNIT (agent-review) — the `[x]` green line sat directly above Gap bullets still asserting the branch was uncovered, the file 7/8, and the suite 530. A closed gap rendered as open is exactly the staleness that misroutes the next work unit, in the file that IS the state source of truth. Marked CLOSED, reasoning kept. (4) OWED, MINOR (premortem) — **silent truncation.** `Composer` caps at `maxLength` and `ChatWorkspace.send()` slices again to `MAX_TOPIC_LENGTH`, both with no counter, warning, or `aria-live`; the API spec puts NO maxLength on `topic` (only `minLength: 1`), so the 500 cap is a purely client-side decision that silently changes what the user asked for and then bills for it. The `.slice()` is itself unguarded — `Composer.test.tsx` asserts only the `maxLength` attribute, so deleting the slice stays green. Secondary: `slice` cuts UTF-16 units, so a paste truncated mid-surrogate puts a lone surrogate into the JSON body. (5) NOTE (agent-review) — removing the skip marker converted "total silence on a blank Ctrl+Enter" from a parked observation into an **enforced contract**; the owed affordance (validation message / retained focus / `aria-live`) is now something a future change must deliberately renegotiate rather than simply add. (6) Refactor applied: extracted `typeTopic(value)` (4 call sites) — previously declined only because a verbatim line-pinned comment blocked it, the constraint this unit removed. Suite after fixes: **539 pass / 0 fail / 0 skip**; typecheck clean.
  - **Pre-existing, not introduced by align-design.** `send()` is untouched by this unit's diff and the unit's test edits were purely additive (no test removed). Recorded here because the branch sits in the idle-composer path this unit reshaped (`Composer` now takes `topicLabel`, `send` is still its `onSend`), and it is one cheap renderer-level test.
- [x] red-selenium **CORRECTED** (2026-07-28) — the original 1.1 contract asserted `[data-testid='generation-generating']` immediately after the type pick, which the user's align-design decision made wrong: "generation starts immediately" is the removal of the mode step, not a POST at type-pick time. That assertion could never pass, so `green-selenium` could not be a remove-marker-only step against it. Rewritten to the agreed flow: pick type → assert the generation surface → assert no mode modal → send a topic → assert exactly one `POST /api/v1/generations` **with the right payload**. Discharges both obligations recorded against the original: (a) the transient-surface flake — waiting on the pending state was unreliable because a fast backend moves past it between polls, indistinguishable from a regression; (b) `assert_no_mode_modal_shown` passing vacuously for any modal-free screen, now backstopped by the positive POST assertion. Skip marker + reason preserved; the reason still predicts a `TimeoutException` from `assert_generation_surface_shown`, which remains accurate. **test-review +5 fixes, one BLOCKING:** (1) `_assert_element_text_equals` takes `(driver, locator, expected, label)` with no default for `label`; the first draft passed 3 args, which raises `TypeError`, NOT the `TimeoutException` the skip reason predicts — the recorded RED prediction was unverifiable and green-selenium would have hit a Python error instead of the documented failure. (2) the surface assertion was presence-only (`_wait_for_visible`), the Selenium equivalent of `isNotNull`, and covered only 1 of 3 scenario-critical elements; now pins breadcrumb text, composer placeholder, composer-starts-empty, send-button label, and send-disabled-while-empty — presence alone would pass on a composer wired to the wrong type or holding a stale topic. (3) the count-only POST assertion never checked the *user's* topic was sent; now asserts the full body by equality. `document_type` is the load-bearing field: the wire value is Cyrillic `доклад` and the app value `doklad` is a backend 422, so this is what pins the picked type reaching the request rather than being defaulted. (4/5) `topic-input`/`topic-send` locators and their expected copy were duplicated across `generate_flow_statements` and `chat_workspace_statements` under diverging names; hoisted into a locators-only `composer_locators.py`. **Hazard found and fixed while reviewing:** `driver.get_log("performance")` **drains** the buffer, so reading count and payload through two separate helper calls would make the second see an empty log and report zero requests for a flow that made one. Base DSL refactored to a single `_matching_requests_to` scan both derive from; `_count_requests_to` kept intact for its two existing callers. **Left alone deliberately:** no post-send transient-state wait — that would reintroduce obligation (a) and require inventing a testid, the exact failure mode of the previous round. The fixed 1s settle in the shared base DSL stays a latent flake source for all three callers; it predates this work, and changing shared timing inside a test review is the wrong place. Acceptance collects **40 tests / 0 errors**. **Review passes on ea80642 (refactor applied 2; agent-review CONCERNS/2; premortem CONCERNS/4 — they converge on 2 of the same):** all four FIXED IN THIS UNIT, because every one of them would have made green-selenium's first live run *unreadable* rather than merely wrong. (1) **a 401 replay was reported as double-billing** — `authorizedRequest` replays verbatim on 401 ("exactly one retry") and `createGeneration` mints its `Idempotency-Key` ONCE per call precisely so the backend collapses that replay onto one generation. `len(requests) == 1` counts wire requests, not generations, so a token expiry mid-flow fails on CORRECT behavior — and its message is the exact headline of the double-billing defect this story already fought, sending the next engineer after a bug that does not exist. Now counts **distinct `Idempotency-Key` values** (new `_request_header` helper — the headers were already on the CDP object, `_request_body` just never read them) and prints the key list on failure. (2) **a disabled-button click is a silent no-op** — `send_topic` clicked a button the assertion immediately above had just pinned as *disabled*, and `_wait_for_visible` waits on visibility, never enabled-ness. Chrome discards such a click without an exception, so any lag before React's controlled re-render yielded a test that typed a topic, clicked nothing, and failed a second later with `got 0`. Now waits on `element_to_be_clickable`, and asserts the composer disappears afterwards (`ChatWorkspace` swaps `Composer` for `Progress` once state leaves idle; `Progress` has no testid, so the composer's absence is the data-testid-only way to pin the swap) — a swallowed click now fails at the click, a broken handler at the swap. (3) **the unit's central claim was unasserted, and the log window was the whole session** — the single `get_log` call sat at the very end, and since it drains, its window covered the live register/verify/login round trip, a full asset load and the polling GETs at `loggingPrefs: ALL`; if the one request we care about were evicted from chromedriver's finite buffer the test would report `got 0` and an operator would revert a healthy build. Worse, **nothing asserted zero POSTs at type-pick time** — the very claim this rewrite exists to defend — so the final `== 1` would pass identically if the POST fired on the type card and the send did nothing. Added a baseline scan between the surface assertion and the send: it enforces the design argument AND narrows the final scan to just the send. (4) **the first live failure would be unreadable** — the first assertion raised a bare `TimeoutException` naming a CSS selector, which three different causes produce identically: auth collapse back to the landing, a genuine routing RED, or a workspace rendered with `state !== 'idle'`. Added a precondition waiting on `chat-panel` (renders in every generation state, unlike the breadcrumb) that re-raises with the failure modes and `driver.current_url`. **PREDICTION SPENT — the more important discovery:** the recorded RED prediction was already void before any of this. `selectType` has set `step='form'`+`mode='auto'` since 916ab0a and the mode modal is deleted, so a `TimeoutException` from `assert_generation_surface_shown` can no longer be produced by any code path. **1.1's Selenium test is therefore not RED at all — production already implements the flow, and `green-selenium` is a first live verification run, not a red-to-green transition.** The skip reason now says exactly that instead of predicting a failure the run cannot produce. Refactor applied 2: extracted a `ComposerAssertionsMixin` (the placeholder/empty/label/disabled block was a line-for-line copy, failure messages included, across scenarios 1.1 and 4.1) and deleted `ChatWorkspaceStatements._given_signed_in`, whose seeded keys were overwritten moments later by `_establish_logged_in_precondition` — dead, and a second place a key rename in `authSession.ts` would silently break. Acceptance collects 40 / 0 errors; ruff clean; all files under 200 lines (189/174/48).
- [x] green-selenium — **PASS on the first live run** (2026-07-28), remove-marker-only: dropped the class-level `@pytest.mark.skip`, rewrote its deferral comment into a past-tense record (the RED prediction was spent at 916ab0a — this was a first live *verification*, not a red→green transition), dropped the now-unused `import pytest`. No production code, no Statements/DSL, no backend touched. `test_should_go_straight_to_generation_with_no_mode_modal` **1 passed in 11.09s**. All six DSL steps held live: workspace reached, breadcrumb `Доклад`, composer empty and prompting, no mode modal, **zero** POSTs at type-pick time, then exactly one distinct-`Idempotency-Key` `POST /api/v1/generations` with body `{document_type: "доклад", topic: <TOPIC>, volume_pages: 5}`. **Infrastructure block below RESOLVED** by standing up a per-worktree stack instead of touching the shared one: new `infra/.env` (BACKEND_PORT=8100, POSTGRES_PORT=5433, REDIS_PORT=6380, FRONTEND_PORT=8081) + `backend/.env` (`GENERATION_PROVIDER=fake`, generated `JWT_SECRET`, dummy `YANDEX_CLIENT_ID`/`SECRET`), brought up under the **distinct compose project name `textery-fe`** (`docker compose -p textery-fe`) so no other worktree's containers can ever be recreated — only `postgres`/`redis`/`backend`, NOT the `frontend` container, whose image would have been a stale artifact. This worktree's code is served by `npm run dev` on **5173**, which is exactly where `acceptance/conftest.py`'s `app_url` fixture already defaults; vite proxies `/api` to 8100 by its own default. **Traps recorded:** (a) `backend/.env.example` is stale — it documents no OAuth vars, but `container/oauth_wiring.py:49-50` `_require`s `YANDEX_CLIENT_ID`/`YANDEX_CLIENT_SECRET` at import, so the backend crashlooped on boot until dummies were added; the example file should be updated. (b) The acceptance run needs `BACKEND_PORT=8100` in the environment — `ApplicationClient` defaults to `localhost:8000`, so without it the live-session step dies with `httpx.ConnectError` (WinError 10061) *before the browser starts*, which looks nothing like a test defect. `FRONTEND_PORT` from `infra/.env` (8081) must NOT be exported — the dev server is on 5173. (c) One transient `SessionNotCreatedException: Chrome instance exited` on the very first invocation, did not recur. **FLAGGED, not touched (remove-marker-only mandate):** the full `tests/frontend` sweep is **23 failed / 17 passed** (7m39s) — the manual-editor suite, the export-control suite, and three auth in-flight tests, all `TimeoutException`. Verified pre-existing: with this unit's change stashed, `test_manual_editor_acceptance.py::TestManualEditorEmptyStateAcceptance` and `test_login_submit_loading_indicator_acceptance.py` fail identically. The manual-editor half is the already-recorded consequence of deleting the mode modal (see align-design finding (7)) and belongs to **scenario 6.1**; the export-control and auth failures are unexplained and want their own investigation before the rest of Story 18's frontend lane is trusted.
- Original block record, kept for the reasoning: **BLOCKED on shared local infrastructure, not on code.** The contract is ready (see the corrected red-selenium entry above) and acceptance collects 40 / 0 errors, but the live run cannot be done safely from this worktree as things stand. Facts, gathered 2026-07-28: (1) a stack is already up — `infra-backend-1` (host port **8100**, started today 05:10), `infra-frontend-1`, `infra-postgres-1`, `infra-redis-1`. (2) This worktree has **no `infra/.env` and no `backend/.env`** (both gitignored, only `.env.example` is tracked), so the running stack was started from another checkout — `git worktree list` shows four sharing this host: `textery` (dev), `textery-be` (story-17-backend), `textery-fe` (here), `textery-rec` (story-5-frontend). (3) **`infra-frontend-1`'s image was built 2026-07-26 17:54** — before every Story 18 frontend commit (all 2026-07-27/28). So it serves a build with no `generation-breadcrumb`, no `GenerationHeading`, and the mode modal still present: running 1.1 against it would fail on a stale artifact, which is exactly the unreadable-failure class the last unit spent a whole pass guarding against. (4) **A rebuild is not safe from here.** Compose derives its project name from the compose file's directory (`infra`), which is the SAME project the running containers belong to — so `docker compose -f infra/docker-compose.yml up -d --build frontend` would *recreate the shared containers* that three other worktrees are using, violating the destructive-action guardrail ("NEVER remove Docker containers you didn't start"). Without an `infra/.env` here it would also fall back to default ports (8000/80) and collide. **Safe path identified, not yet taken:** `vite.config.ts` already defaults its `/api` proxy to `http://127.0.0.1:8100` — the running backend — and `acceptance/conftest.py`'s `app_url` fixture already defaults to port **5173**, Vite's own. So `npm run dev` inside `frontend/` serves THIS worktree's code on the port the acceptance suite already expects, proxying to the shared backend, touching no container. Scenario 1.1 needs only `POST /api/v1/generations`, which has existed since story 1, so the shared backend's branch does not matter for this test. Note `GET /health` on 8100 returns 404, so the health path is named something else — confirm the backend is actually serving before blaming the test. Owner decision needed on whether to run the suite this way or to stand up a per-worktree stack with its own `infra/.env` and project name.
- [x] demo — watched live in a visible Chrome (2026-07-28), `1 passed in 27.16s` (vs 11.09s headless; the delta is the injected 1.2s delays). Demo changes were the standard temporary pair — commented out `--headless=new` and a `_demo_delay()` at the top of `_wait_for_visible` — plus **one deviation worth recording: the window was shrunk from `1400,1000` to `1280,800`.** The skill calls for this generally, and it bit here: the headless size is close enough to the host display that a decorated visible window may not be placeable, and Chrome then dies on connect (`session deleted … not connected to DevTools`) at the first interaction, which reads as a test failure rather than a window-geometry problem. **Also deviated from the skill's step 3 deliberately:** did NOT run `/stop-backend` + `/run-backend`. This worktree's backend is the per-worktree docker stack (compose project `textery-fe`), and the skill's backend scripts target the local process/shared stack — stopping it would have violated the destructive-action guardrail for a clean-DB benefit this test does not need (it registers a fresh user per run via `issue_live_session`). Both files reverted via `git checkout --`; verified zero `DEMO` markers remain and the working tree is clean. **Scenario 1.1 is now complete.**

### Scenario 1.2: A generating document shows progress
- [x] red-selenium — new `test_generating_state_acceptance.py` (53) + `generating_state_statements.py` (151), class-level `@pytest.mark.skip`. Flow: pick doklad → **throttle the network** → send topic → assert both halves of the workspace say "in progress" → assert neither terminal surface is up → assert a status GET is genuinely on the wire. **The 1.1-owed transient-surface hazard is DISCHARGED, not deferred:** the stack runs `GENERATION_PROVIDER=fake`, whose `generate` returns a canned string with zero latency, and `useGeneration.submit` fires its first status check the instant the POST resolves — so the pending state can be single-digit milliseconds wide while `WebDriverWait` polls at 500ms. Fixed by *widening the window*, not retrying into it: CDP `Network.emulateNetworkConditions` latency (the existing `NetworkThrottleMixin`, `SLOW_LATENCY_MS=2500`) plus the fact that `submit` sets `pending` **synchronously before** awaiting the POST, so the generating surface is up before the first byte leaves and stays up for ≥2× latency. That is literally the scenario's "When the result is not ready yet". Deliberately NOT done by slowing the fake provider (production code; every backend acceptance test would pay) and NOT by blocking the poll URL (that is scenario 4.3's mechanism — a poll *error*, a different state). **NOT a red-to-green transition, and this was verified rather than argued:** `generation-generating` was added by 1.1's green-frontend explicitly as this scenario's subject, so the behaviour already ships. The test was run live 3× (10.9s / 12.8s / 12.0s, all pass) — green-selenium is remove-marker-only with nothing owed. **The RED protocol still earned its keep — two defects in the *contract* that only this layer can see, both found by running, both wrong in my first prediction:** (1) `.chat-panel h3` carries `text-transform: uppercase` and Selenium reports RENDERED text, so the panel reads `ХОД ГЕНЕРАЦИИ`, not the source's `Ход генерации` — jsdom applies no CSS, so the renderer suite asserts the source casing and the two layers legitimately expect different strings for one element; (2) `Progress.ChatMsg` puts its `✦` avatar on its own line in `.text`. Prediction run 1 named the right assertion, right type, right reason (panel value guessed; the doc-surface value was read off the component and held) but the wrong text; run 2 still mismatched *invisibly* and needed a `repr` dump to see it. **Reuse:** `pick_document_type_for_doklad` + `send_topic` + `CHAT_PANEL` + `GENERATIONS_PATH` were extracted out of `GenerateFlowStatements` into a new `generation_flow_actions.py` (64) mixin rather than copied — 1.2 is 1.1's opening gesture plus different assertions, and subclassing 1.1's Statements would have dragged in an unrelated scenario's assertions. `GenerateFlowStatements` 189→159. **1.1 re-run live after the extraction: still passes (10.7s).** Assertion depth beyond the one-clause spec line: both panels pinned by *equality* (the doc area renders a placeholder in 3 of 4 states differing only by copy, so presence-only would pass on the **idle** placeholder — the one state meaning the send never happened); `doc-body`/`doc-error` asserted absent so "progress" cannot be satisfied by a finished screen, which doubles as the backstop naming the real cause if the throttle ever silently stops applying; and the status-GET scan proves the spinner is *watched* rather than eternal (pre-empting 4.4). That scan loops-and-accumulates because the first check cannot exist yet when the DOM assertions finish (it is issued only after the throttled POST resolves) **and** because `driver.get_log` drains — a single scan would have reported a healthy client as one that never polls. **Not asserted, deliberately:** the `В обработке` status badge (third generating indicator) has no `data-testid` and class/tag locators are forbidden. Acceptance collects **41 tests / 0 errors** (baseline 40); ruff clean (the 9 pre-existing `F401`s are all in files this unit never touched); all files under 200L. **test-review +4 fixes, two of them substantive:** (1) `assert_client_is_awaiting_the_result` collected GETs via `_matching_requests_to`, which matches on a URL **substring**, then asserted bare truthiness — so a history load of the *collection* endpoint `/api/v1/generations` (no run id at all) satisfied an assertion whose own docstring and failure message promise `GET {path}/<id>`. It was weaker than its stated contract and would not have bitten the regression it exists to catch. Now: exactly one distinct polled path, pinned by equality to `f"{GENERATIONS_PATH}/{generation_id}"`, with the id parsed as a UUID; verified against six inputs (passes single + repeated real polls; rejects a collection GET, a query-string listing, `/undefined` as the id, and two different ids polled). The id stays unpinned *by value* with the reason written in the code — it is minted backend-side and returned in the create POST's **response body**, which `Network.requestWillBeSent` does not carry, so no stricter option exists from the performance log; its *format* is now pinned. (2) `send_topic` typed, clicked, **and** asserted the composer had been replaced — a Then hidden inside a When that neither test class could see; split into `send_topic` + `assert_send_started_a_run`, now called explicitly from both 1.1's and 1.2's test class (the clickability wait stayed in the action: it guards the gesture, not the outcome). (3) De-duplication: `CHAT_PANEL` was defined in both `chat_workspace_statements.py` and the new `generation_flow_actions.py` (whose own docstring argues against exactly that) — duplicate deleted, shared one imported; the "element is absent" wait was written out four times across Statements — hoisted to `_assert_not_visible` on `BaseFrontendStatements`, all four call sites delegated. (4) Reached back into 1.1: `assert None not in keys` on `Idempotency-Key` passed on `""` or a constant placeholder, which would silently collapse two genuine creates into one distinct key — under-counting and hiding the very double-billing regression that assertion defends; every key must now parse as a UUID. **Left alone with reasons:** the accumulating `while` in the poll scan is not a violation (it computes no expected value; it is retry-with-accumulation forced by `get_log` draining); the `В обработке` badge still has no testid, and adding one is a production edit that green-selenium's remove-marker-only mandate forbids. **The 1.1 live test was edited by this review** (the `assert_send_started_a_run` split) and the reviewer could only verify it by collection — so it was re-run live here: **`test_generate_flow_acceptance.py` 1 passed in 12.64s**. Acceptance re-collects 41 / 0 errors. **Review passes on 310bd84 (refactor applied 4; agent-review CONCERNS/3; premortem CONCERNS/3 — they converge on the poll matcher):** refactor extracted the duplicated UUID predicate into `statements/uuid_format.py` as a *predicate* not an assertion (the two call sites have genuinely different failure messages — one prints the whole key list, the other names the offending path — and a shared `assert_is_uuid` would have flattened both into one generic wording, which would have been a weakening), flattened `TOOLBAR_BUTTON_ARIA_LABELS` from a `dict[str, list[str]]` whose every value was a one-element list, removed a tautological `assert element.is_displayed()` after a `visibility_of_element_located` wait (the wait's `TimeoutException` IS the assertion), and corrected the throttle mixin's docstring. (1) FIXED IN THIS UNIT (agent-review) — **the documented backstop was unreachable by call ordering.** `assert_no_result_shown`'s docstring calls itself the backstop for a silently-unapplied throttle, but the test called it *after* `assert_generating_state_shown`; if the latency stopped applying, the run completes, the generating surface never renders, and the wait raises a bare `TimeoutException` naming a CSS selector first — the exact misattributed failure this scenario exists to eliminate — so the backstop could only ever run on a path where it had nothing left to catch. Reordered to absence-first; it costs nothing when the throttle IS applied (neither terminal surface can be up yet, both checks pass immediately). (2) FIXED IN THIS UNIT (**both passes**, the substantive one) — test-review's tightening moved the substring fragility rather than removing it: cardinality was asserted over everything *containing* `/api/v1/generations`, so any second legitimate GET under that prefix (a history/collection load — `historyApi.ts:102` is that shape) fails 1.2 with either "got 2 distinct paths" or, if the collection load lands first, "a path with no single run id is a collection load" — asserting the client is not polling when it demonstrably is, i.e. a future unrelated feature turns this scenario red and sends someone into `useGeneration`. Now a `_is_status_poll_path` shape filter (`{GENERATIONS_PATH}/<one segment>`) runs *inside* the scan loop, so the loop keeps scanning until it has a request of the right shape; the downstream equality assert that was a shape-check wearing equality's clothes is gone, the UUID check stays. Predicate verified against 6 paths (real poll, collection, trailing slash, `/status` sub-path, `/undefined`, unrelated resource). (3) FIXED IN THIS UNIT (agent-review) — the poll budget was `WAIT_TIMEOUT_SECONDS` (5s, a constant tuned for DOM waits) for an event that *structurally* cannot occur before `SLOW_LATENCY_MS` (2500) elapses, with nothing tying the two together; the three live runs passed with about one spare iteration, and raising the latency would have made a healthy client report as one that never polls. Now `POLL_SCAN_TIMEOUT_SECONDS = max(WAIT_TIMEOUT_SECONDS, 3 × SLOW_LATENCY_MS / 1000)`, and `network_throttle_mixin` records at the constant's definition site that 1.2 needs a wider window than 4.2/2.1 (they need one follow-up click; 1.2 needs a full DOM assertion pass) so lowering it cannot silently reintroduce the flake. (4) File split at the cap — the fixes pushed `generating_state_statements.py` to 205L; locators and expected copy moved to a locators-only `generating_state_locators.py` (44), statements now 182. (5) **OWED, the load-bearing untested premise (premortem, credible):** the whole design rests on `useGeneration.submit` setting `pending` **synchronously before** awaiting the POST, and nothing asserts it. With latency L the surface is up across `[0, ~2L]` if the premise holds and `[L, ~2L]` if it does not, and `_wait_for_visible` cannot tell those apart — so any refactor that inserts an await ahead of `setState('pending')` (topic validation, a token refresh, moving the set inside the `try`) gives the user a dead UI for the whole POST, a second click, and a duplicate create, with 1.2 green throughout. Wants an assertion that the surface is visible *while the create POST is still in flight*. (6) OWED, lower severity (premortem) — the polled id is shape-checked, not equality-checked, so polling a *stale but well-formed* UUID (a lagging ref, a memoized closure) reads as healthy and would render another run's document. The comment says the id is unobservable from `Network.requestWillBeSent`, which is true, but `Network.responseReceived` + `Network.getResponseBody` on the create POST make the equality check buildable — cost/benefit call, and this Selenium layer is the only one that can see the wire. (7) NOTED — the class-level skip means none of this has run in CI once; confidence is live runs on one machine against one per-worktree stack. Verification after all fixes: 1.2 and 1.1 re-run live together, **2 passed in 20.49s**; acceptance collects 41 / 0 errors; ruff clean on every touched file; `generating_state_statements.py` 182L, `generating_state_locators.py` 44L (pre-existing `statements/auth_statements.py` sits at 203L, untouched here). **Pre-existing, re-confirmed by the refactor pass and NOT this unit's regression:** `test_manual_editor_acceptance.py` fails identically with this work stashed — `open_manual_editor_for_doklad` still clicks `MODE_CARD_MANUAL`, which story 18 deleted; it belongs to scenario 6.1.
- [x] red-frontend — live vitest RED at the `DocumentGenerationFlow` seam. New `DocumentGenerationFlow.generatingState.test.tsx` (111, `it.skip`). **Subject: the load-bearing premise the whole 1.2 Selenium design rests on and nothing asserted** (OWED item (5) from red-selenium's review passes) — that `useGeneration.submit` sets `pending` **synchronously before** awaiting `createGeneration`. Deliberately NOT the naive "pending renders the generating surface": that gap was checked and is real — `useGeneration.test.ts:44` asserts `state === 'pending'` only *after* `await act()` over a **resolved** `createGeneration`, so it passes whether the state set is before or after the await, and no renderer test referenced `generation-generating` at all. **Mechanism:** full `App` render with `generationApi`/`documentApi` mocked, driving the real UI (CTA → `type-card-doklad` → `topic-input` → `topic-send`) — the sibling `DocumentGenerationFlow.documentType.test.tsx` pattern, reused not duplicated — with `createGeneration` stubbed to a promise that has **no resolve function in existence**, so the generating surface can only be observed if it rendered without waiting for the request. **Behaviour ships today, so this is a characterization/regression pin and was bite-verified instead of predicted-red:** moving `setState('pending')` to the line after `await createGeneration(...)` produces `TestingLibraryElementError: Unable to find an element by: [data-testid="generation-generating"]` at the `getByTestId` line, 1 failed; restored, `git diff --stat src/features` empty. Prediction matched on run 1 across all four fields (type / message / location / status). **Correction to the brief, worth reading:** the premise handed to red-agent said the regression would stay green everywhere. It does not — breaking it gives **2 failed / 538 passed**, because the sibling "bills one generation for a double Ctrl+Enter" case stubs `createGeneration` the same never-settling way and the still-mounted `Composer` accepts the second keydown. So the double-billing *consequence* already had a guard; the *cause* had none. The distinction is diagnostic, not academic: that sibling fails with "expected 1 call, got 2", which sends an engineer to `Composer`'s missing in-flight guard — the wrong file — while this one fails on the missing surface, which is both the real defect and the exact assertion the Selenium contract rests on. The overstated sentences in the file's comments were corrected rather than left standing. **test-review +3 fixes:** (1) the surface assertion was `expect(getByTestId('generation-generating')).toBeInTheDocument()` — existence-only, satisfied by an empty div, on a surface whose every string is category-1 "you define it"; replaced with `within(generating)` sub-assertions anchoring `/^Готовим ваш доклад$/` and the exact wait-time line. (2) the control arm pinned only that `topic-input` is mounted, so the "before" half was an absence a blank doc area would also satisfy; strengthened with the idle-only heading `Опишите тему доклада`, making before/after text-vs-text. (3) scenario depth — the scenario says *progress* but nothing asserted the run badge (`В обработке`) or the progress rail (`ИИ пишет доклад`), both rendered from the identical pending branch; added as the same claim, not a second scenario. Every expected string was read out of `DocArea.tsx`/`Progress.tsx`/`ChatWorkspace.tsx`, and because `it.skip` compiles but never runs (a mistyped en dash in `1–2` was the live risk), the file was run once with the marker off to prove the new strings are real, then restored. Suite **539 pass / 0 fail / 1 skip** (baseline 539/0 — exactly the expected +1 skip); `npm run typecheck` clean in the `-b` form. **Review passes on d4f34b7 (refactor NO-OP; agent-review CONCERNS/5; premortem CONCERNS/3):** refactor declined the one real candidate — the shared render-and-drive setup with `DocumentGenerationFlow.documentType.test.tsx` — for reasons worth keeping: a shared harness costs ~25-30 lines to remove ~15, both files are far under the cap, only a 3-line prefix is actually common (the control-arm assertions sit *between* the type click and the topic change), and hiding `mockReturnValue(new Promise(() => {}))` behind a helper is exactly the indirection that lets someone later "simplify" it to a resolvable promise without reading the warning that it is the mechanism. (1) FIXED IN THIS UNIT (agent-review) — the file's headline claim was **false and the commit body's correction had not reached it**: line 15 still read `Nothing asserts it.` while the sibling double-Ctrl+Enter test was written expressly on this premise and fails on the same production edit. Corrected in place — the consequence was guarded, the cause was not, which is a diagnosis difference and not a coverage one. This mattered because that sentence and the progress lead are what a future reader acts on. (2) FIXED IN THIS UNIT (agent-review) — the protection scope was overclaimed: of the three named refactors, **a token refresh is structurally out of reach at this seam**. Refreshes live in `authorizedRequest` inside `generationApi`, which the file mocks out wholesale, so a refresh added where refreshes actually live is invisible here — and no layer guards that case (Selenium measures the surface, not when the first byte leaves). Comment corrected to claim only what it catches. (3) FIXED IN THIS UNIT (**premortem**, same class as the Selenium poll matcher, twice) — `screen.getByText('ИИ пишет доклад')` searched document-wide while the two `DocArea` assertions above it were correctly `within`-scoped; any second surface carrying that string (a run-history strip, a toast — both plausible in this story) makes it throw on MULTIPLICITY and fail naming a string rather than the premise. Now scoped to `chat-panel`. The badge cannot be scoped the same way — no testid, and adding one is a production edit this phase forbids — so it is recorded rather than worked around. (4) FIXED — the closing `topic-input` absence was commented as "the other half of the double-billing protection", re-creating the co-firing this unit exists to separate; reframed as the render-ordering fact (the surface swap IS the state set becoming visible), with the consequence left to the sibling. (5) Assertions re-verified by running the file once unskipped after the edits (**1 passed**), marker restored. (6) **OWED, the most serious finding (premortem, CREDIBLE, production defect — not fixable in a RED phase):** `useGeneration.submit` calls `stopPolling()` *before* the await and then unconditionally starts polling after it, with **no run-token, epoch ref, or AbortController checked post-await**. Sign out during the in-flight window (the header is on screen in every state) and `closeToLanding` → `reset()` → `stopPolling()` clears an interval that does not exist yet; when the POST resolves, polling starts anyway on a logged-out session and on `completed` writes content + state. `DocumentGenerationFlow` does not unmount on logout (it renders `LandingPage`), so the unmount cleanup never fires either — the 5s interval runs for the life of the tab. `useFlowNavigation.ts:104-106`'s comment asserts the opposite in prose ("`closeToLanding` already stops the poll" — true for a running poll, false for a create still in flight). Same shape on any second submit overlapping a first: `intervalRef.current` is overwritten without `clearInterval`. No test calls `submit`, then `reset()`/logout, then **resolves** the create and asserts `getGeneration` was never called; `grep unmount` finds nothing in any `useGeneration` test. Wants its own red/green pair. (7) OWED (premortem) — **scenario 1.2 now has two guards and zero of them execute**: `it.skip` here and a class-level `@pytest.mark.skip` on the Selenium file. Nothing fails when a skip marker outlives its step; the suite reports 1 skipped and exits 0. Cheapest form of a guard is a lint rule or reporter check failing on `it.skip` outside a declared allowlist. (8) OWED, minor (agent-review) — the run-badge assertion is not new depth: `ChatWorkspace.test.tsx:51` already pins it for `pending`; only the progress rail was genuinely unasserted. Noted in the file. (9) NOTED (premortem) — the unit adds no production guard, and double-billing remains prevented **solely** by the `pending` render swap. `ExportControl.tsx:54` already carries a real in-flight lock in this codebase, so the pattern exists and is simply not applied here. Suite after fixes: **539 pass / 0 fail / 1 skip**; typecheck clean; file 131L.
- [x] green-frontend — **marker-removal only, zero production code changed**, exactly as the RED step characterized: `useGeneration.submit` sets `pending` synchronously before awaiting `createGeneration`, so the generating surface already renders and the pin is a regression guard, not a missing behaviour. `it.skip(...)` → `it(...)` in `frontend/src/app/__tests__/DocumentGenerationFlow.generatingState.test.tsx` (131 → 127L), plus de-staling the recorded-failure comment: the `RED, recorded failure.` prefix became present-tense ("What this pin catches, bite-verified rather than observed failing because the behaviour already holds"), the spent paragraph about running the file once with the marker off was dropped, and the trailing `green-frontend removes this marker only.` line removed. Every assertion, expected string, mock, and setup line byte-identical; `git diff --stat` shows the single test file and nothing under `src/features`. Suite **540 pass / 0 fail / 0 skip** (baseline 539/0/1 — exactly the expected +1/−1), 126/126 files; `npm run typecheck` clean. The `Not implemented: navigation to another Document` lines remain pre-existing jsdom stderr noise, not failures. **Still owed from the RED step's review passes and NOT closed here** (a marker flip cannot close them): (6) the `useGeneration.submit` post-await race — no run-token/epoch/AbortController checked after the create resolves, so a sign-out or second submit during the in-flight window starts a poll that outlives the session; wants its own red/green pair. (7) skip markers that outlive their step fail nothing — this unit removed the vitest one, but the Selenium file's class-level `@pytest.mark.skip` is still live until `green-selenium`.
- [x] red-frontend-api — new `frontend/src/features/generation/api/__tests__/generationApi.generatingPoll.test.ts` (127L, `it.skip`), one case: *asks for the generation it was given and reports it as still running*. **Subject picked from the gap the two prior 1.2 units structurally cannot see:** `DocumentGenerationFlow.generatingState.test.tsx` mocks `generationApi` **wholesale**, so URL, method, headers, init, response mapping — everything below that module — is invisible at the renderer seam. Two facts here were load-bearing for "a generating document shows progress" and pinned by nothing: (1) `getGeneration(id)` interpolates `id` into the path — both existing `getGeneration` cases read `calls[0][1]` (headers) or the mapped result, **neither reads `calls[0][0]`**; (2) the in-progress body is not the body the existing mapping test uses — `generations_get.yaml` documents `content` as present only when `completed`, so a real mid-generation answer has no `content` key (the shape `?? null` exists for), and `status` — the field the whole poll state machine branches on — is unpinned through this function, since the hook's own tests mock this module out. Mechanism: `vi.stubGlobal('fetch', …)` + `saveSession`, matching the six sibling `api/__tests__` files (the repo has no MSW server, so the template's MSW pattern was not used). **Characterization pin, bite-verified rather than predicted-red** (same shape as this scenario's `red-frontend`): passes on run 1; deleting `${id}` from `generationApi.ts:81` gives `AssertionError: expected '/api/v1/generations/' to be '/api/v1/generations/gen-42'` at the URL assertion, **1 failed / 37 passed** — the whole `api/` + `hooks/` suite stayed green but this one test. Prediction matched on all four fields. The unguarded symptom is scenario 1.2's exact screen: a generating state that never leaves, pixel-identical to a working one and reading as a slow backend. **test-review +4 fixes / 4 rejected:** (1) the request was asserted field-by-field while the response was compared recursively, so of the four keys `performRequest` puts on the fetch init, three were pinned and `signal` was not pinned at all; replaced with a whole-object `expect(init).toEqual({method, signal: expect.any(AbortSignal), headers, body: undefined})`, which additionally fails on any stray init key (a `credentials`/`responseType` appearing on a read) and keeps the abort signal wired — the thing that releases the socket under the 5s poll, whose absence leaks abandoned connections silently. Bite-verified too: deleting `signal,` from `httpClient.ts:136` fails with `-"signal": Any<AbortSignal>`; restored clean. (2) the header claimed the Selenium test "cannot say which generation it asked about" — **false**, `generating_state_statements.py:166-182` asserts exactly one distinct polled path *and* that its last segment is a UUID, explicitly rejecting the id-less shape; rewritten to the true gap (Selenium pins the id's *format*, not its *value* — the id is minted in the create POST's response body, which `Network.requestWillBeSent` does not carry — and the class is skipped anyway). (3) consequently "drop the `${id}` and every test in the repo still passes" was qualified to "every test that actually runs". (4) the multiplicity comment credited `toHaveBeenCalledTimes(1)` with pinning that a hook tick does not fan out; the test calls `getGeneration` directly and never touches the hook, so it only catches an internal retry inside `send`/`authorizedRequest` — reworded to what the seam can observe. Rejected: deleting/folding sibling tests (removing assertions is forbidden; one-scenario-per-file is the convention here, `generationApi.documentType.test.ts` the precedent), a 19-file shared authed-fetch harness (real duplication, but cross-cutting green files — `/refactor`'s call), and fixture-placement style churn. Suite **540 pass / 0 fail / 1 skip** (baseline 540/0/0 — exactly the expected +1 skip); `tsc -b` clean. **Note for `green-frontend-api`:** the 7-field `toEqual` strictly subsumes `generationApi.test.ts:54-64` and largely subsumes its 2-of-7 mapping test — a consolidation candidate, but for a refactor commit, not the green one. **OWED item (6) triaged, not closed:** the `useGeneration.submit` post-await race does **not** belong to this step — wrong seam (the defect is the hook's state machine; `generationApi` is stateless and correct, it is the *caller* that starts a poll on a dead session, so it needs `renderHook` + fake timers), wrong scenario (a poll outliving what started it is nearer 4.3/4.4 than 1.2), and it needs real production code, which would smuggle a behavior change into a characterization unit whose green is marker-removal-only. It has now survived three units unclosed and wants its own explicit `red-*`/`green-*` pair.
- [x] green-frontend-api — **marker-removal only, zero production code changed**, the third such green in this scenario and for the same reason: the pin characterizes shipping behaviour, so there was nothing to make pass. `it.skip(...)` → `it(...)` in `frontend/src/features/generation/api/__tests__/generationApi.generatingPoll.test.ts` (127 → 124L), plus de-staling the recorded-failure comment — dropped the past-tense `RED, ` prefix (now `Bite-verified: deleting ${id} … fails this at the URL assertion`, still naming the exact AssertionError) and removed both now-spent `green-frontend-api removes the marker only` lines. Every assertion, expected string, mock body, `beforeEach`/`afterEach`, and the `inProgressWire` fixture byte-identical. Isolated run **1 passed**; full suite **541 pass / 0 fail / 0 skip across 127 files** (baseline 540/0/1 over 126 files). The vitest reporting quirk noted at red is confirmed from the other side: a fully-skipped *file* is omitted from the aggregate file/test totals, so the skip was invisible in the full-run summary and only showed when the file ran alone — with the marker gone, both the file count and the test count advance by one. `tsc -b` clean. **This closes the RED premortem's CREDIBLE 1** — for the one work unit between the two steps, scenario 1.2's poll-identity fact had zero executing guards at any layer (the renderer pin mocks `generationApi` wholesale; the Selenium class is skipped), and the red commit's "the symptom it now catches" was true only prospectively. It executes now. Also fixed in this unit: the red entry's `generationApi.ts:80` citation was off by one (the interpolation is line 81), caught by agent-review. **Still open and NOT touched here:** the `documentType` wire→app asymmetry now carrying a passing pin (premortem CREDIBLE 2 — `createGeneration` sends `WIRE_DOCUMENT_TYPE[...]` while `getGeneration` returns the raw `'доклад'`; wants a round-trip test that goes red today), `GenerationStatusWire.content` declared non-optional against a fixture that omits the key, `error_message` pinned away by the whole-object `toEqual`, and OWED (6), the `useGeneration.submit` post-await race — now four units unclosed.
- [x] align-design — the generating surface (mockup 01/`05-chat-pending.html`; story 18 has no mockups of its own, Spec `[S]`, same basis as 1.1's align-design). **Two real diffs, no colour churn:** the app's UI Kit deliberately supersedes the mockups' violet gradient with flat brand blues (`index.css:41-50` says so), so token values are NOT drift and were left alone; likewise the `1fr 320px` + `order` layout flip is story 01's own Figma-driven decision (`01/progress.md:148-154`), not a regression. (1) **Copy naming a type it does not know** — `DocArea.tsx` pending title `Готовим ваш доклад` and `Progress.tsx` pending line `ИИ пишет доклад` were both hardcoded, sitting on the one screen whose badge and run carry the type the user actually picked: verbatim the defect `/design-review` failed `Composer.tsx:22` for in 1.1. Fixed at the same seam and by the same precedent as `topicFieldLabel`: new exhaustive `DOCUMENT_TYPE_ACCUSATIVE` + a private gendered `DOCUMENT_TYPE_POSSESSIVE` (`ваш доклад` / `ваше эссе` — the possessive agrees, so it cannot be a constant in the template) and two composers `generatingTitle()` / `writingProgressMessage()` in `shared/documentTypes.ts`; `DocArea` and `Progress` now take `documentType` alongside `label`, joined in `ChatWorkspace` where the pair already lives. Deliberately NOT derived from `WIRE_DOCUMENT_TYPE`, which holds the same four strings today — that map is the backend's vocabulary and the file already argues against coupling the two directions. (2) **Spacing** — `.doc-placeholder p { margin-bottom: 24px }` was unconditional, but mockup 05 (generating) has no margin and mockup 07 (failed) has 24px *because a button follows*; the rule left 24px of dead space under the generating screen's last line, off-centring the one state with nothing below it. Moved to `.doc-placeholder p + .cw-btn { margin-top: 24px }`, which keeps the failed panel identical. **Guard:** new `ChatWorkspace.generatingCopy.test.tsx` (55L) renders through `ChatWorkspace` (the prop join is what a caller gets wrong) with `referat` — a `doklad` assertion would pass against the very hardcode it exists to rule out — plus a `sochinenie` case for the neuter possessive. **Bite-verified:** restoring the `Готовим ваш доклад` literal fails both cases; restored, both pass. `/design-review` **PASS** first run, zero findings on the generating surface (full string inventory of mockup 05 re-diffed). Coverage on the touched set: `Progress`/`ChatWorkspace`/`documentTypes` 100% across the board, `DocArea` 100% stmts/lines/fns. Suite **543 pass / 0 fail / 0 skip** across 128 files (baseline 541/127); typecheck clean in the `-b` form; oxlint clean; prettier clean on the touched files (4 unrelated files were already unformatted and were left alone); every touched file under 200L (136/88/56/105/157/55). **Review passes on 4bebcaf (refactor applied 1; agent-review CONCERNS/4; premortem CONCERNS/3 — they converge on the same top finding):** refactor extracted a `renderGenerating(documentType)` helper from two 10-prop renders and looked the label up from `DOCUMENT_TYPE_LABELS` instead of hand-passing it (a case naming `referat` while labelling it `Сочинение` would have been testing a caller mistake); it declined three candidates with reasons — dropping `DocArea`'s `label` (it originates as a public prop, and `documentTypeLabelFromWire` proves a label is not always the table's value), merging `DOCUMENT_TYPE_ACCUSATIVE` into `WIRE_DOCUMENT_TYPE`, and collapsing the three declension tables into one record-of-forms. (1) FIXED IN THIS UNIT (**both passes, the converging one**) — **the `доклад` literal did not disappear, it moved to a layer with no executing guard.** `generating_state_locators.py:26,43` pin the generating copy by *equality* and are scenario 1.2's witness that the state is up; since this unit they transcribe the output of `generatingTitle('doklad')`/`writingProgressMessage('doklad')`, and the new vitest guard deliberately used only `referat`/`sochinenie` — so **no executing test rendered doklad's generating copy at all**. Editing `DOCUMENT_TYPE_ACCUSATIVE.doklad` or either template kept all 543 green while invalidating both Python constants, and the breakage would have surfaced only at `green-selenium`, as a text mismatch on the assertion whose failure message means *"generating state not shown"* — pointing at polling rather than at a copy string. Closed from both ends: a `doklad` case added to `ChatWorkspace.generatingCopy.test.tsx`, and the locators' comment now names the TS source and the test that ties them. (2) FIXED IN THIS UNIT (agent-review, credible) — **the partial fix made one panel contradict itself mid-session.** The chat panel keeps every step on screen and the workspace is not remounted across a run, so `ИИ пишет реферат` was followed one state later by the still-hardcoded `Пишу доклад`, and a `Готовим ваше сочинение` by `Не удалось сгенерировать доклад` — two document types named to the same user about the same run, which is worse than being consistently wrong. (3) FIXED IN THIS UNIT (agent-review) — **the deferral rationale recorded at the behavior commit was false for three of the four remaining hardcodes.** Only `Создать новый доклад` needs adjective agreement; `Пишу доклад` and `Не удалось сгенерировать доклад` are accusative (the table this unit added) and `Опишите тему доклада` is genitive (shipped at 1.1). All three now decline via new `writtenProgressMessage()` / `generationFailedTitle()` / `topicPromptTitle()`, guarded by a new `ChatWorkspace.transcriptCopy.test.tsx` (71L) that drives ONE workspace through pending → completed → failed → idle with `referat` and asserts no `/доклад/` survives in the panel — the generating-copy suite structurally cannot see this, it never leaves `pending`. **Bite-verified:** restoring `Пишу доклад` fails it (`Unable to find an element with the text: Пишу реферат`). (4) FIXED IN THIS UNIT (premortem) — **the CSS half shipped with no guard of any kind.** `p + .cw-btn` silently stops applying if the failed panel's button is renamed or wrapped (exactly what a later `/refactor` does), and jsdom observes no CSS, so the whole suite stays green. What a renderer test *can* pin is the selector's premise; `error-reset` is now asserted to carry `cw-btn`. (5) OWED (agent-review) — the same margin removal also changed the **idle** placeholder, a third state the commit message accounted for neither way. Same improvement by inspection (idle has no button either) but unstated and unmockup'd; scenario 1.1 owns that screen. (6) OWED (premortem, product) — `useFlowNavigation.ts:57` turns an unrecognised wire type into `doklad` (`documentTypeFromWire(...) ?? 'doklad'`); before this unit that fabrication surfaced as a breadcrumb label, now it would render `Готовим ваш доклад` as a statement of fact about someone's эссе. The in-file comment argues that path cannot reach the chat surface today — and only the comment enforces it. (7) MINOR (agent-review) — `DOCUMENT_TYPE_ACCUSATIVE` is exported with no consumer outside its own file while `DOCUMENT_TYPE_POSSESSIVE` is private on the stated principle that it is meaningless outside the phrase it builds; the same argument now applies to both. Suite after all fixes: **547 pass / 0 fail / 0 skip** across 129 files; typecheck, oxlint, prettier and ruff clean; files 155/93/60/75/71/55L. **OWED, out of this unit's scope:** (a) `DocArea.tsx:47` `Создать новый доклад` is the one remaining hardcode, and it is genuinely blocked: the adjective agrees in gender (`новый доклад` / `новое эссе`), which none of the three tables covers. (b) `DocArea` is 75% branch — three untaken `??` fallbacks (`volumePages ?? '—'`, `content ?? ''`, `error ?? default`), all reachable (every prop is `| null` from `useGeneration`'s own state) and all pre-existing; the coverage pass proposed four steps for them but they are a backfill item, not scenario 1.2's, and their greens would be no-ops. (c) `lucide-react` still not installed, so mockup 05's `sparkles` avatar renders as the text glyph `✦` — repo-wide, recorded at 1.1.
- [x] green-selenium — **PASS on the first un-skipped run**, remove-marker-only: dropped the class-level `@pytest.mark.skip`, rewrote its deferral comment (written as a forward promise) into a past-tense record, dropped the now-unused `import pytest` — the same three-part shape as 1.1's green-selenium. No production code, no Statements/DSL/locators, no backend. `test_generating_state_acceptance.py` **1 passed in 41.85s**. As the red step predicted, this was a first live *verification*, not a red→green transition: `generation-generating` has shipped since 1.1's green-frontend and red-selenium already ran the test live 3×. **The align-design fallout risk did not materialise** — `EXPECTED_GENERATING_DOC_TEXT`/`EXPECTED_GENERATING_PANEL_TEXT` still match now that the copy is computed (`generatingTitle('doklad')`/`writingProgressMessage('doklad')`), and both panels passed their equality assertions; the vitest doklad pin added in `fc1fba7` is what keeps that true. **Stack:** brought the per-worktree compose project `textery-fe` back up (postgres 5433 / redis 6380 / backend 8100, `GENERATION_PROVIDER=fake`) plus `npm run dev` on 5173; the other worktree's `infra-*` containers on 8000/5432/6379 were left alone. Both 1.1 traps held and are re-confirmed: `BACKEND_PORT=8100` must be in the environment or `ApplicationClient` dies with `httpx.ConnectError` against another worktree's backend before the browser starts, and `FRONTEND_PORT` (8081, the container) must NOT be exported. **Timing anomaly, now correctly UNEXPLAINED:** the run took 41.85s against red-selenium's 10.9/12.8/12.0s on the same stack and code. The two causes first recorded here — a cold vite/browser start, or a loaded machine — were both refuted by this same unit's own figures (agent-review): the full sweep ran 319.55s against the 1.1 baseline's 459s, so the machine was ~30% *faster*, not loaded; and of red-selenium's three runs the FIRST was the fastest, so a cold start costs nothing here. Attaching a plausible-sounding cause closed a question that is still open — it is left open now. What the premortem DID trace and close: the 2500 ms throttle window is not at risk. It is ~5s wide (`SLOW_LATENCY_MS` twice — the create POST plus the first status round trip) and every assertion inside it is a fast path that resolves immediately; the wall-clock cost is browser/vite startup and the poll-scan's `REQUEST_LOG_SETTLE_SECONDS` sleep, both OUTSIDE the held window, and `POLL_SCAN_TIMEOUT_SECONDS` is already derived from `SLOW_LATENCY_MS` so the two cannot drift. **Full `tests/frontend` sweep: 23 failed / 18 passed** (319.55s) — the failure set is identical to the baseline recorded at 1.1's green-selenium (manual-editor ×14, export-control ×6, auth in-flight ×3) and the pass count rose by exactly one, this test. Still pre-existing, still untouched; the manual-editor half belongs to scenario 6.1. **Review passes on a2dd214 (no `/refactor` step for this phase; agent-review CONCERNS/4; premortem CONCERNS/3+1 remote):** two of agent-review's findings were factual errors in this very entry and are FIXED IN THIS UNIT — (1) *CI-shaped* was false: **no workflow runs `acceptance/` at all** (`frontend-ci.yml` is vitest-only and path-filtered to `frontend/**`; `backend-ci.yml`'s pytest step runs from the backend directory), so un-skipping made the class *executable*, not *executed*, and a never-invoked file announces itself less than a skip marker did — corrected in both this entry and the test's own header comment; (2) the timing attribution above, refuted by this unit's own numbers and now left open. (3) MINOR, FIXED — the rewritten header said the unit *changed nothing else* while the same commit rewrote that comment and dropped `import pytest`; the in-file record is what a reader lands on first, so it now says all three. **OWED, all three premortem CREDIBLEs — real gaps in what this now-live scenario promises to catch:** (a) **the perpetual spinner still ships green.** The test observes only `useGeneration`'s *immediate* first status check and ends at `release_the_result` with no assertion that the run ever resolves; delete the `setInterval` reschedule or break the `completed` branch and every assertion here still passes — the exact ambiguity the DSL's own docstrings claim to eliminate. Scenario 2.1, which would catch it, is entirely `[ ]`. Cheapest guard: assert `doc-body` appears after the throttle is released. (b) **the green run cannot prove it drove this checkout.** `conftest.app_url` hardcodes `5173` as an ambient default and asserts nothing about what is serving there, while `vite.config.ts` sets `strictPort: true` — so a second worktree's dev server refuses to bind, the failure is easy to miss, and 5173 belongs to whoever got there first, proxying `/api` to *its* target. The port ritual is recorded as tribal knowledge in prose; sibling fixtures (`expired_code`, `provider_secret`) assert their environment contract loudly and this one does not. `MEMORY.md` already records one wrong-branch incident of this shape. (c) **poll amplification is invisible to the assertion that watches polls** — `polled_paths` is a *set*, so N concurrent intervals on one generation still measure 1; `_count_requests_to` already exists for exactly this cardinality claim and story 18's own scenario 2.2 is about firing something exactly once.
- [x] demo — `TestGeneratingStateAcceptance` run visibly (non-headless, 1.2s delay before every visibility wait): **1 passed in 29.89s**. Chrome shown at 1280×800 rather than the headless 1400×1000, per the skill's note that a full-size window cannot be composited with decorations and Chrome dies on connect. **One demo-specific patch worth recording:** `SLOW_LATENCY_MS` was temporarily raised 2500 → 12000. The demo delay adds ~1.2s to each of the five visibility waits inside the held window, which at 2500 ms would have consumed the pending surface mid-pass and failed the demo for reasons that have nothing to do with the behaviour being shown — the window is the mechanism that makes this scenario observable at all, so a demo has to widen it in proportion to its own slowdown. All three patched files (`conftest.py`, `base_frontend_statements.py`, `network_throttle_mixin.py`) reverted via `git checkout --`; working tree verified clean and both constants confirmed back at `--headless=new` / 2500. The backend was NOT restarted as the skill's step 3 asks: this worktree's backend is the `textery-fe-backend-1` container brought up for green-selenium and already fresh, and `/stop-backend`+`/run-backend` target a local process, so running them would have risked another worktree's stack for no gain.

### Scenario 2.1: A completed generation opens automatically in the editor
- [x] red-selenium — analytical RED (live run deferred to green-selenium, same shape as 1.1). New `test_auto_editor_transition_acceptance.py` (63) + `auto_editor_transition_statements.py` (156) + `auto_editor_transition_expectations.py` (70) + conftest fixture. Flow: pick doklad → send topic → assert a run is in flight → **instrument the page to record any further user gesture** → assert the editor opened by itself → assert the read-only result is gone → assert the editor holds the generated text and is editable. **Existence check before writing (the RED is genuine, not a coverage pin — unlike every step of 1.2):** `POST /api/v1/documents/from-generation`, the only new endpoint `endpoints.md` names, exists in neither `backend/` nor `frontend/src`; `useGeneration.runPollAttempt` handles `completed` by setting content + state and nothing else; `DocArea` renders `completed` as a read-only `doc-body`; `DocumentGenerationFlow` mounts `ManualEditor` only for `mode==='manual'`, which `selectType` never sets on the create path. **PREDICTED FAILURE:** `TimeoutException` from `assert_editor_opened_by_itself` on `[data-testid='manual-editor']`, message "expected a completed generation to become the editor by itself, but [data-testid='manual-editor'] never appeared within 30s". **ACTUAL:** not yet observed — the class carries a `@pytest.mark.skip` and the live run is green-selenium's, exactly as 1.1 did it; the prediction is recorded verbatim in the test header and is what green-selenium must reconcile against. **Deliberately NOT throttled, unlike 1.2:** 1.2's subject was the *transient* pending surface (a few ms wide against a zero-latency fake provider, hence the CDP latency), 2.1's is the *terminal* state — once the editor is up it stays up, so waiting for it is not a race, and throttling would serialise ~2.5s onto each of three requests and push a healthy run toward the timeout for no observability gained. **Reuse, not copy:** `GenerationFlowActionsMixin` (`pick_document_type_for_doklad`/`send_topic`/`assert_send_started_a_run`), `DOC_BODY`, `GENERATING_SURFACE` from `generating_state_locators`, `MANUAL_EDITOR`/`EDITABLE_CONTENT`. Verified: acceptance collects **42 tests / 0 errors** (baseline 41); ruff clean on every touched file; the expected-content constant machine-checked against the fake provider's `FAKE_DOKLAD_TEXT` rather than transcribed by hand. **test-review +3 fixes / 1 rejected** (clusters P and S had already reported clean in the interrupted prior session and were not re-run; A and Se ran here): (1) **the read-only-replaced assertion ruled out two of DocArea's three non-editor surfaces** — `GENERATING_SURFACE` was unasserted despite living in a module the file already imported. An auto-transition that opens the editor *without tearing the run down* leaves the spinner standing underneath: the user is told the text is still being written while already holding it, and the poll loop that outlived its own result keeps hitting the backend. The test passed on exactly that leak. (2) **the editor was asserted to hold the text but never to be editable** — a read-only render carrying `data-testid='editor-content-area'` satisfied every other assertion, i.e. the completed view wearing the editor's name; needing one more action before you can type is precisely the extra step 2.1 exists to remove. Now `contenteditable == "true"` by equality, not truthiness. (3) file split at the cap — the two fixes pushed Statements to 213L; constants and the two instrumentation scripts moved to `auto_editor_transition_expectations.py` (the `generating_state_locators.py` precedent), 156 + 70. **Rejected:** dropping `.strip()` from the text comparison — the lane's shared `_assert_element_text_equals` strips, Selenium's `.text` is browser-normalised innerText whose edge whitespace carries no product meaning, and deviating here buys flakiness not strictness; the docstring's overclaim ("no tolerance is taken") was corrected instead to say the comparison is character-exact *through* the document, which is the claim that matters since the interior blank lines are what the expected constant exists to pin. **OWED to green:** `EXPECTED_AUTO_EDITOR_BREADCRUMB = "Доклад"` fixes a decision *ahead* of production — the mode chip is hardcoded in `ManualEditorBreadcrumb.tsx`, so green must make it conditional or this assertion fails on the breadcrumb rather than on the missing transition, which is the misattributed-failure class this lane has spent three units eliminating. **Pre-existing, flagged not fixed:** repo-wide `ruff check` reports 8 unused-`pytest` F401s in files this unit never touched (including `test_chat_workspace_acceptance.py`). **Review passes on 9d27e7b (refactor applied 1; agent-review CONCERNS/5; premortem CONCERNS/5+2 remote — they converge on the wire gap):** refactor replaced a hand-rolled `_wait_for_visible`→`.text.strip()`→`assert ==` with the lane's shared `_assert_element_text_equals`, which the method's own docstring already named and then wrote out again; the failure message got *stronger* (the expected text is now printed beside the actual). It declined 5 candidates, two worth keeping: collapsing the three `_assert_not_visible` calls into a loop would flatten three different diagnoses ("the result was not replaced" / "the run failed and the editor opened over the error" / "the run was never torn down") into one generic wording, and wrapping the gesture-watch scripts in a helper is precisely what invites a later simplification to collapse the `None` case into the `== []` case, silently turning a lost witness into a clean run. Four findings FIXED IN THIS UNIT, all test-side, because each would have made green-selenium's first live run false-pass or unreadable: (1) **premortem CREDIBLE — the "no extra click" witness could observe an already-finished transition and pass vacuously.** `assert_send_started_a_run` is an `invisibility_of` wait on the composer: it returns the moment the composer is gone and establishes nothing about the run still being pending. Against the zero-latency fake provider the whole run can complete and the transition occur *before* `execute_script(ARM_INTERACTION_WATCH_SCRIPT)` runs — the watch then arms on the post-transition page and `events == []` passes having observed an empty window. It defeated the reload check from the same direction: a reload *preceding* the arm installs the script on the fresh document, so `__acceptanceUserEvents` exists and is `[]` and both assertions read clean. **The irony is exact:** the commit message dropped throttling because "2.1's subject is terminal, so waiting for the editor is not a race" — true of the subject, false of the *arming window*, which is as transient as 1.2's subject was. Fixed by holding the create POST open across that one step (`NetworkThrottleMixin`) and arming only once `GENERATING_SURFACE` is visible, so the window is provably non-empty; new `NoExtraClickWitnessMixin` (97). Option (b), CDP `Page.addScriptToEvaluateOnNewDocument`, was **rejected with the reason recorded in the docstring** — it runs only on a *new* document, so on the healthy in-place path it would never execute and the read returns `None`, converting the reload check into a permanent false failure. Cost is ~2.5s on the create POST alone; the transition and the lazy chunk stay unthrottled, and the class docstring now draws the subject-vs-instrument line explicitly. (2) **premortem CREDIBLE + agent-review, converging — there was no wire assertion at all,** so both a poll loop outliving its own result and a conversion firing once per tick were invisible. Hiding the spinner and calling `stopPolling()` are independent lines in `useGeneration.ts`: a green that switches to the editor branch and forgets the teardown passes all three absence checks while `POLL_INTERVAL_MS=5000` hammers the backend for the life of the tab, and a conversion driven off a live tick mints a duplicate document every 5s — invisible on screen, since React collapses the editor mount, and surfacing as "users report 4-9 identical copies". The docstring *claimed* to catch this and did not. New `AutoEditorTransitionWireMixin` (154): exactly one distinct-`Idempotency-Key` `POST /documents/from-generation` (a 401 replay is not double-billing — 1.1's precedent), every key a UUID, body pinned by **equality** to `{"generation_id": <the id actually polled>}` rather than by format, because the polls come out of the same drain; plus `assert_the_poll_loop_stopped`, which sleeps `POLL_QUIET_SECONDS=7` (> the 5s interval, so a live one *must* tick) and asserts zero further status polls. Bound is **zero, not N** — once the editor holds the text there is nothing left to ask about, and a tolerance would hide the leak inside it. **The drain hazard was respected structurally rather than by discipline:** `_drain_requests` + a pure `_requests_matching` were extracted to a new `request_log.py` (93), `_matching_requests_to`/`_count_requests_to` became thin wrappers, and the conversion assertion takes POSTs *and* polled ids from ONE drain which doubles as the quiet period's window-opener. `_is_status_poll_path` moved to `generation_flow_actions.py` as public `is_status_poll_path`, now shared with 1.2 instead of copied; this also pulled `base_frontend_statements.py` 209 → 131, under the cap it had quietly been over. (3) **agent-review — the 30s budget was spent on the wrong wait.** `AUTO_TRANSITION_TIMEOUT_SECONDS=30` is justified partly by the ManualEditor lazy chunk, but it was consumed waiting on the `MANUAL_EDITOR` *shell* — and `LOADING_SKELETON` renders **inside** that shell, so the shell appears before Tiptap loads and the two waits that actually need the chunk (breadcrumb, `EDITABLE_CONTENT`) fell back to the shared 5s. A slow chunk therefore failed on `editor-content-area` and green would have read it as "the conversion produced no text". Now the skeleton is waited out at the 30s budget, which is the chunk-arrival signal; chosen over raising the shared `WAIT_TIMEOUT_SECONDS`, which would slow every unrelated failure in the suite. (4) **agent-review + premortem, RECORD-ONLY** — two constants are pinned by exact equality and neither was ever observed, but only one was recorded as owed. The breadcrumb note now names **both** deletions green owes (the hardcoded mode chip *and* the `me-breadcrumb-sep` separator — today it renders `Доклад · Ручной режим`). `EXPECTED_GENERATED_TEXT` gained three: its blank lines were derived analytically from the `inline*` schema and `HardBreakNode`, but the assertion compares against Selenium's browser-normalised innerText over rendered `<br>`s and Tiptap appends its own `ProseMirror-trailingBreak` — so green-selenium must **characterize it, not satisfy it** (observe the real `.text` first; a whitespace-only mismatch is a test-side correction, a word difference is product feedback), because the cheapest path to green on a mismatch is otherwise to bend the product to a Selenium artifact; it is a hand transcription of `FAKE_DOKLAD_TEXT` tied by nothing, crossing a lane boundary so the constant's owner will never see this test (1.2 solved the analogue with a named tying test — **owed, and deliberately not written here**, since the constant lives in `backend/` and the tie would be a backend-lane test, outside this worktree's file ownership); and `runtime.py` defaults `GENERATION_PROVIDER` to `gigachat` when unset, so a stack started without `backend/.env` runs the real provider and fails this equality with a message naming the conversion. **OWED, not closed (premortem CREDIBLE 5, the structural one):** the class-level skip fails nothing and **no workflow runs `acceptance/` at all** — `frontend-ci.yml` is vitest-only and path-filtered, `backend-ci.yml`'s pytest runs from the backend directory — so removing the skip makes this test *executable*, not *executed*; the only thing that ever runs it is a person deciding to. And because this is an analytical RED, nothing yet distinguishes "correctly fails" from "errors in the fixture / typo'd testid": **green-selenium must record the observed failure text against the prediction before implementing** — a RED never seen red cannot certify its green. Verification after all fixes: acceptance collects **42 / 0 errors**; ruff clean on all 9 touched files (only the 8 pre-existing F401s remain); MRO smoke-checked (every inherited method resolves on `AutoEditorTransitionStatements`, and 1.2's module still imports after the `is_status_poll_path` move); files 79/158/154/97/98/93/92/167/131, all under 200.
- [x] red-frontend — **live vitest RED, and a genuine one** — the first in this story since 1.1's `red-frontend`; all four of 1.2's steps were characterization pins on shipping behaviour, this one asserts a transition no code performs. New `DocumentGenerationFlow.autoEditorTransition.test.tsx` (144, `it.skip`). Renders `App` with `generationApi`+`documentApi` mocked and drives the real UI (CTA → `type-card-doklad` → `topic-input` → `topic-send`); after a poll observes `completed`, asserts `manual-editor` appears with **no further gesture**, `doc-body` gone, `generation-generating` gone. **Seam chosen deliberately, against the two this lane has been burned by:** `renderHook` on `useGeneration`/`useFlowNavigation` pins hook *state*, and hook state is not where the defect is — `runPollAttempt` already sets `state='completed'` correctly today; what is missing is that nothing turns that state into a surface, so a hook assertion could go green with the screen still read-only, verbatim the "pins a contract one layer below where the defect lives" mistake recorded against 1.1's red-frontend-api. `generationApi`/`documentApi` is `red-frontend-api`'s seam and cannot see a surface at all. The transition is a render decision spanning three files (`useGeneration` sets state only; `DocArea` renders `completed` read-only; `DocumentGenerationFlow` gates `ManualEditor` on `mode==='manual'`), so only a render can observe it. **Predicted failure:** `TestingLibraryElementError`, message `Unable to find an element by: [data-testid="manual-editor"]` + printed DOM, at the `findByTestId` call, line 90, 1 failed / 0 passed in the file. **Actual failure:** `TestingLibraryElementError: Unable to find an element by: [data-testid="manual-editor"]` at `DocumentGenerationFlow.autoEditorTransition.test.tsx:89:33`, 1 failed / 0 passed; printed DOM shows `<div class="doc-body markdown-body" data-testid="doc-body"><p>Готовый текст доклада</p></div>` beside `Создать новый доклад` — the generation completed, the text arrived, the surface stayed read-only. **Comparison:** type MATCH, message MATCH, counts MATCH, **location MISMATCH** — predicted line 90 (the `expect`), actual 89:33 (the `await findByTestId`; the throw anchors at the call, not at the `expect` consuming it). Re-predicted to 89 and re-ran unchanged: all four fields matched on run 2. A wrong line attribution, not a wrong code path — corrected rather than waved through, and the recorded comment now cites the call *positionally* so it cannot rot. **test-review +4 fixes / 3 rejected** (cluster A only; P/S/Se do not apply to a vitest renderer test): (1) the editor assertion was `findByTestId` followed by a tautological `toBeInTheDocument()` — `findByTestId` already throws on absence, so the pair asserted only that *a node with that testid mounted*, satisfied by an empty div and equally by **the completed read-only view wearing the editor's name**, which is precisely the extra-step defect 2.1 exists to remove; replaced with `within(editor)` → `editor-content-area` + `contenteditable === 'true'` by equality (the renderer-side twin of the fix the Selenium contract took one unit earlier; the attribute is real today, from `useManualEditorInstance.ts:72`, and jsdom already relies on it in `ManualEditor.formatting.test.tsx`). (2) nothing asserted the editor opened **for the type the user picked**; added `editor-breadcrumb` anchored `/^Доклад$/`, pinned to the same constant the Selenium contract fixed — the current render was *measured*, not assumed, and is `Доклад · Ручной режим`, so this is now the cheap layer that catches the deletion green already owes in `ManualEditorBreadcrumb.tsx` (both the hardcoded chip at :28-33 and the `me-breadcrumb-sep` span at :27). Failing on that string here costs a second; failing on it at green-selenium costs a live stack and reads as a missing transition. The comment states the limit honestly — `doklad` is the only `available: true` card, so this arm cannot rule out a hardcoded default; that is the sibling `documentType` test's job. (3) `getGeneration` — the call that drives the whole transition — had **no argument assertion at all** while `createGeneration` got the full exact-args treatment; a poll issued against a stale or hardcoded id renders another run's document with everything else green. Now `toHaveBeenCalledWith(GENERATION_ID)`. (4) added `toHaveBeenCalledTimes(1)` **but rejected the detector's rationale for it** — "a second call means the interval survived" is false at this seam: under real timers the 5s `POLL_INTERVAL_MS` never elapses in a millisecond-scale test, so teardown is structurally unobservable here. Kept for what it does pin (the transition does not re-read what it is already holding — scenario 2.3's claim), and the comment above the `generation-generating` absence was **corrected**: it had claimed that absence proves "the run was torn down, not left spinning underneath", which it does not — the surface swap hides the spinner whether or not `stopPolling()` ran. It now names the Selenium contract's `assert_the_poll_loop_stopped` (which sleeps past the interval on purpose) as the only layer that can see it. **Rejected:** asserting `export-control-trigger` inside the editor (pins a toolbar affordance, not the claim, and broadens the file into ManualEditor's own suite) and loosening the breadcrumb to a substring (the exact defect the lane catalogues, and it would silently pass the owed chip deletion). **Deliberately left, and respected by the review:** "with the generated content loaded" is NOT asserted here — asserting text inside `editor-content-area` would force a mechanism decision in RED, since `vi.mock` auto-mocking only stubs *existing* exports and a not-yet-existing `createDocumentFromGeneration` would return `undefined` and fail for a reason unrelated to the transition; that half belongs to `red-frontend-api` and to the Selenium contract, which already pins the exact text. `documentApi.createDocument`/`getDocument` are stubbed to never-settling promises on purpose, so this test cannot depend on persistence resolving. The generating surface is 1.2's subject and is not re-pinned (the first poll observes completion, so no timer is advanced). **RED re-observed after the review edits** (marker off, then restored): same failure, same call, now at :97:33 — every new assertion sits *after* that wait, so none of them moved where the test fails. Suite **547 pass / 0 fail / 1 skip** across 130 files (baseline 547/0/0 across 129 — exactly the expected +1 skip, +1 file); `npm run typecheck` clean in the `-b` form (no `@ts-expect-error` needed — the test calls no signature that does not exist yet); oxlint and prettier clean; 144L. **Review passes on d7a8d1a (refactor NO-OP; agent-review CONCERNS/3; premortem CONCERNS/4 — all six fixable findings FIXED IN THIS UNIT):** `/refactor` declined the shared render-and-drive harness for the third time, and **its central reason got stronger rather than weaker at three call sites**: the common surface did not grow (still a 3-line body prefix plus a 3-line `beforeEach` prefix; everything after diverges, and this file needs a wholly different `beforeEach` where `createGeneration` *resolves*), but `mockReturnValue(new Promise(() => {}))` now means **two opposite things** — in `generatingState`/`documentType` it is *the mechanism* (the surface can only be observed if it rendered without awaiting), here it is the opposite claim (*the test must not depend on it resolving*). One helper would erase that distinction at exactly the sites where the distinction is the contract, and would edit two passing siblings to serve one skipped file for a net line loss near zero. (1) FIXED, the severe one (agent-review) — **the test's own comment pointed green at breaking a live, un-skipped acceptance test.** It said the `Ручной режим` chip and `me-breadcrumb-sep` "must go", on the premise that story 18 left the mode chip decorating a choice nobody is offered. False, and the codebase says so in the file the test's header cites: `useFlowNavigation.ts:14` records that `mode` is always `'auto'` on the create path but **`'manual'` survives the history-open path** (`openDocumentFromHistory`, :58) — which is under `manual_editor_statements.EXPECTED_DOKLAD_BREADCRUMB = "Доклад · Ручной режим"`, consumed by `test_manual_editor_acceptance.py`, **not skipped**. The Selenium contract two commits earlier had this right ("green makes the mode chip *conditional*; the manual path keeps its own constant"); this file's prose contradicted it. Since tests are read-only in green, an engineer following the comment in the test they are trying to satisfy would delete the chip, break a suite they may not edit, and discover it three layers away. Comment corrected to conditional-on-the-auto-path, naming the constant that must keep passing. (2) FIXED (agent-review) — **none of the assertions below the failure point had ever been executed**: the RED throws at `await findByTestId('manual-editor')`, so the `contenteditable` equality, the breadcrumb regex, both absence checks and both `getGeneration` assertions had never evaluated in any state of the codebase, and two are hand-typed Cyrillic literals — the exact class of typo the *previous* red in this lane caught by running the file once with the marker off (a mistyped en dash in `1–2`). Done here by the honest route available: a throwaway test mounted `ManualEditor` directly on the history-open shape and passed on `manual-editor`, `editor-content-area` + `contenteditable="true"`, the breadcrumb reading exactly `Доклад · Ручной режим` (which proves the hand-typed `Доклад` is the real string **and** that `/^Доклад$/` fails today for the right reason rather than a typo), the new content form, both absences, and `createDocument` not-called; scratch deleted, record kept in the file header. **Still unverifiable before green:** that these hold on the *auto* path (no such path exists) and the two `getGeneration` count assertions (they depend on a transition nobody has written). (3) FIXED (agent-review) — "anchored to the same constant" described a link that does not exist: `EXPECTED_AUTO_EDITOR_BREADCRUMB` is Python in `acceptance/`, this file has an independently typed `/^Доклад$/`, a TS file cannot import it, and if the Python constant changes this test silently keeps asserting the old string — verbatim the hazard `documentTypes.ts` warns about. Reworded to *parallel hand-maintained pins*, "edit both or neither". (4) FIXED (premortem CREDIBLE, **and the red-agent's stated objection was overruled**) — **green could open an EMPTY editor and pass every assertion.** `GENERATED_TEXT` was only the mock's payload, never read back out of the DOM; a `ManualEditor` mounted with empty content satisfied all five surface assertions, and `queryByTestId('doc-body')` being null *actively removes the one surface still showing the text* — a cosmetic gap turned into data loss ("it deleted my report"). The deferral to red-frontend-api and the Selenium contract did not hold: the former sits at a seam that by its own header cannot see a surface, the latter is skipped, and 2.3 lands *after* green-frontend 2.1 — leaving a window where a blank-editor implementation is fully green. The objection (that `vi.mock` auto-mocking returns `undefined` for a green-added `createDocumentFromGeneration`, so the assertion could fail for an unrelated reason) was judged not to survive: the assertion is about the DOM after the transition, which green must reach by whatever mechanism it picks, and green owns mocking what it introduces. (5) FIXED (premortem CREDIBLE) — the file gave `getGeneration` both called-with and called-times and made **zero assertions about `documentApi`**, whose stubs never settle; two green shapes passed identically — mounting `ManualEditor` with no `existingDocumentId`, which makes `useDocumentInit` fire `createDocument` and mint a **second, empty document** unrelated to the generation, or wiring the conversion but never resolving a `documentId`, whose consequence `useDocumentInit`'s own comment states verbatim ("`handleSave` returns on its first line, so the Save button becomes a permanent no-op and the status line sits on 'Создание документа…' forever"). `createDocument` is now pinned not-called; the second shape is stated in-file as **still indistinguishable at this seam**. (6) FIXED as an honest record, arm deliberately NOT forced (premortem CREDIBLE) — the comment claimed poll-loop teardown was covered by the Selenium `assert_the_poll_loop_stopped`; that class is `@pytest.mark.skip` **and no CI workflow runs `acceptance/` at all**, so the nominated guard was nominal twice over. The incident is not only load: a late poll resolving after the user starts typing has `runPollAttempt` write `content` again, **overwriting their edits mid-sentence**. The comment now states plainly that teardown has *no executed guard in any suite*, names the incident, and describes the fake-timer arm exactly (advance past `POLL_INTERVAL_MS` after the editor mounts, assert `getGeneration` still at 1) — unwritable until the editor can mount, so **owed to green, in this file**. **OWED, not closed (premortem CREDIBLE 1, structural, and it subsumes the rest): nothing enforces skip removal.** `frontend-ci.yml` runs `npm run test:coverage` and never inspects the skip count, coverage thresholds cannot catch it (green's production lines are covered by the sibling render tests regardless), and this lane has a live precedent of a skip outliving its step. A skipped guard guards nothing. Verification after all fixes: RED re-observed with the marker off — same failure, same call — marker restored; suite **547 pass / 0 fail / 1 skip** across 130 files; typecheck (`-b`), oxlint, prettier clean. **File is 200L, exactly at the cap** — the next edit to it must split first.
- [x] green-frontend — **the first green in this story that writes production code** (1.2's four were all marker-removal-only). The transition now exists. **`DocumentGenerationFlow.tsx` (124→137):** inside the existing `step==='form'` block, `const generatedContent = flow.generation.state === 'completed' ? flow.generation.content : null`, and the editor branch fires on `mode === 'manual' || generatedContent !== null`, with `generatedContent={generatedContent ?? undefined}` on `ManualEditor`. Reusing that branch rather than adding a second was deliberate — the auto path and the history path want the *same* editor with the same lazy boundary and the same error recovery, and a second copy of that JSX is exactly what `/refactor` would delete next unit. `ChatWorkspace` is now simply what renders when neither holds, so `doc-body` and `generation-generating` disappear **by unmount, not by a hide flag**. **`ManualEditor.tsx` (122→152):** new optional `generatedContent?: string`, whose *presence* (`const fromGeneration = generatedContent !== undefined`) is the single discriminator for the whole auto path. Content is seeded in a `useEffect` guarded by a `seededRef`, keyed on `[editor, generatedContent]` — `editor` is null on first render (Tiptap builds in an effect) so it cannot be a render-time call, and an unguarded re-run would overwrite what the user has typed with the original generation. **`useDocumentInit.ts` (94→107):** new `fromGeneration?: boolean`; the effect returns early when set, because *both* existing branches are wrong for this path — there is no id to GET, and `createDocument` would mint a second empty document. **`ManualEditorBreadcrumb.tsx` (37→55):** new **required** `showManualModeChip: boolean` wrapping the separator span *and* the chip in a fragment; required rather than defaulted because `ManualEditor` is its only caller, so there is no read-only call site to break. Optional-with-default on the two `ManualEditor`/`useDocumentInit` props was **forced, not chosen** — ~20 test call sites construct `ManualEditor`, and a required prop is a `TS2554` in files green may not edit (the same constraint, and the same resolution, as 1.1's `green-frontend-api`). **The five recorded obligations:** (1) breadcrumb **conditional, not deleted** — `showManualModeChip={!fromGeneration}`; the history-open path passes no `generatedContent`, so `Доклад · Ручной режим` still renders and the un-skipped `manual_editor_statements.EXPECTED_DOKLAD_BREADCRUMB` keeps passing. The trap is written into the prop's doc comment, naming that constant and its test, so the next person to look at the chip sees it *before* deleting. (2) `createDocument` must not fire — discharged by the `fromGeneration` early return, at the layer that owns the decision rather than by declining to mount the editor. (3) no empty editor — and this is why the gate is `content !== null` rather than `state === 'completed'` alone: `doc-body` is the last surface still showing the text and it goes away with the workspace, so a completed generation that somehow carries no text **keeps the read-only surface** instead of losing it silently. (4) the `vi.mock` auto-mock hazard is discharged **by design, not by care** — the transition introduces **no new API export**, so there is nothing for the auto-mock to stub as `undefined`; the content travels as a prop out of state `useGeneration` already holds, and both `documentApi` stubs stay never-settling with the test depending on neither. (5) the fake-timer poll-teardown arm — **deliberately deferred, with the reason**: the test file sits at exactly 200L (the one spare line went to de-staling the RED comment and had to be re-tightened back), so adding the arm means *splitting a test file*, a structural edit green is not permitted to make. It is now writable (the editor mounts), so it belongs to a red step of its own; the incident it guards — a late poll calling `setContent` over what the user is typing — is unguarded in every suite today. **Marker + comment:** `it.skip` removed, nothing else; no assertion, setup or teardown touched. Two stale phrases de-staled ("RED, observed live…" → "The RED this was written against, observed…"; "Green removes this marker" → "That path now exists"); the diagnosis, the printed-DOM evidence and the scratch-verification record are intact. File back to 200L. **One shared-harness change that deserves scrutiny — `frontend/src/test/setup.ts` (27→39): `configure({ asyncUtilTimeout: 5000 })`.** The test passed alone and **failed reproducibly in the group run**, at `findByTestId('manual-editor')`, printed DOM showing `Загрузка редактора…`: the transition was firing and Testing Library's 1000ms default was expiring on the lazy Tiptap/ProseMirror chunk (~1.4s alongside siblings, well under 1s alone) — a pass/fail line drawn by machine load, on an `await` the test's own comment already says "covers a real chunk load". Fixed in the harness rather than the test (it is config, not a test file) and it weakens nothing — a `findBy*` whose element never arrives still fails, just slower — but it raises the failure latency of **every** genuinely-broken `findBy*` in the suite. Suite **548 pass / 0 fail / 0 skip** across 130 files (baseline 547/0/1 — exactly the expected +1/−1); `npm run typecheck` clean in the `-b` form; oxlint (`--max-warnings=0`) and prettier clean; no `acceptance/` or `backend/` file touched. **OWED, and the single most important thing not done here: the auto-opened editor has no `documentId` and therefore cannot save** — `handleSave` returns on its first line. `POST /api/v1/documents/from-generation` exists nowhere, so the choice was between wiring nothing and firing the wrong POST; the 2.1 red test explicitly cannot distinguish this state from correct wiring and says so. Recorded verbatim in `useDocumentInit`'s new param comment. That wire is `red-frontend-api`/`green-frontend-api`'s step, next. **Also owed:** `align-design` — the auto-path breadcrumb now renders a single chip in a row styled for two, with `me-breadcrumb-sep` gone, and nobody has looked at that surface; `green-selenium` — its `EXPECTED_AUTO_EDITOR_BREADCRUMB = "Доклад"` is now satisfiable, while its `assert_the_poll_loop_stopped` remains the only nominated teardown guard and is still in a skipped class no CI workflow runs. Scenarios 2.2/2.3 untouched by design — nothing fires a conversion yet, so "exactly once under racing polls" has no subject, and the editor is populated from state in hand rather than a re-read, which is 2.3's direction but not its contract.
- [x] red-frontend-api — **live vitest RED, genuine, and the only new endpoint in story 18 finally has a client contract.** New `frontend/src/features/generation/api/__tests__/documentApi.fromGeneration.test.ts` (150, 2× `it.skip`) + a throwing `createDocumentFromGeneration` stub and a `DocumentFromGenerationResult` type in `documentApi.ts` (156→180). **Existence check before writing:** `grep -rn "from-generation|createDocumentFromGeneration" frontend/src` found only *forward-reference comments naming this very step as the owner* — `DocumentGenerationFlow.autoEditorTransition.test.tsx:20` and `useDocumentInit.ts:20` — plus `ManualEditor`/`useDocumentInit`'s unrelated `fromGeneration` boolean prop. No client function existed. **Contract pinned** (`api-specs/documents_from_generation.yaml`): `POST /api/v1/documents/from-generation`, body `{ generation_id }`, the **required** client-generated `Idempotency-Key` header, and the response mapped off the shared story-5 `DocumentResponse` + the two additive fields `generation_id` (nullable) and `title`. **Two tests, not one, and the second is the whole reason:** "201 first conversion and 200 idempotent replay map identically" is not observable in a single call. The 200 test's kill power is stated honestly in its own comment rather than implied — `send`/`request` branch on `res.ok` alone, so it passes for free on today's transport; it is a pin against a green that adds its own `status === 201` check, which is the natural mistake when writing a *created* mapper. test-review considered two strengthenings and rejected both: re-asserting the distinct key restates test 1's claim, and "assert status is not consulted" is not expressible — the absence of the check is observable only as this test passing at 200, which is what it already measures. **The stub interpolates its arguments into its throw message** because `tsconfig.app.json` sets `noUnusedParameters: true` — a parameter-ignoring stub fails `tsc`, and an `_`-prefix rename is something green would have to undo. Side effect: the prediction became strictly checkable, since the message names the exact arguments each test passed. **PREDICTED FAILURE:** `Error: not implemented (11111111-1111-4111-8111-111111111111, from-generation-abc)` and `…, from-generation-xyz)`, both thrown at `documentApi.ts:165` from test lines 80 and 124, 2 failed / 0 passed — the stub throws before reaching fetch, so no fetch assertion is evaluated. **ACTUAL:** byte-identical messages, `documentApi.ts:165:9` from `:80:26` and `:124:28`, `Tests 2 failed (2)`. **COMPARISON:** type MATCH, message MATCH, location MATCH, counts MATCH — all fields on the **first** run, no re-prediction needed (contrast this scenario's `red-frontend`, where the throw anchored at the call rather than the `expect` and the line had to be corrected). **test-review +1 fix / 0 rejected** (cluster A only; P/S/Se do not apply to an api-seam test): the request assertion was four sequential reads destructured off one captured call, three of them per-field on the same `init` object — an **open** form that admits any extra key the client invents and left `signal` pinned by nothing. Now one closed whole-`init` `toEqual`, the same fix commit `136aead` applied to `generationApi.generatingPoll.test.ts` on this seam. `expect.any(AbortSignal)` is the one surviving non-exact matcher and earns it in-comment: the `AbortController` is built inside `withTimeout` (`shared/api/httpClient.ts:96`) with no API returning the instance, so its identity is unreachable from the test — **and the detector's literally-prescribed fix (a closed `{method, headers, body}`) would have FAILED**, because the transport always passes `signal`. Body compared as the serialized string, safe at exactly one key and it additionally pins that the body is serialized at all. **The review refused to hand green an unexecuted assertion:** both tests being `it.skip` means the rewritten `toEqual` had never run, so it temporarily un-skipped them against a faithful stub (**2 passed** — the assertion is satisfiable, the `signal` matcher and serialized-body form are both correct), then bit it by dropping the `Idempotency-Key` header and watched test 1 fail alone on that exact line. Both temporary edits reverted. **Recorded process scar, worth more than the fix:** the first bite used a `perl -0pi` substitution that silently matched the header line in **`createDocument`** — the first of two structurally identical call sites in the file — leaving the from-generation call untouched. The suite stayed green, which was read as "the bite did not land" rather than as success, and that is what surfaced the damage to an unrelated working function. A regex bite on a file with two identical call sites is not a bite. **Deliberately NOT asserted, so nobody reads coverage into this file that isn't there:** the 404 / 409 / 422 / 401 / 500 branches, which map through the shared `send` → `describeFailure` stack that is already tested; the one client-specific 409 subtlety (an idempotency 409 must not surface as a lost-update message) is already pinned by `documentApi.createConflict.test.ts`. A from-generation-specific error test — that a 409 "generation not completed" does not become `VersionConflictError` — is a **real gap and an owed follow-up step**, not something to smuggle in here. **Scope held:** 2.2 keeps call-cardinality/in-flight dedup, 2.3 keeps which surface consumes the body; this step is method/path/body/header/response-mapping only. Full frontend suite **548 passed / 0 failed / 2 skipped** (131 files), `tsc -b` clean, test file 150 lines.
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 2.2: The auto-transition fires the conversion exactly once
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 2.3: The editor is populated from the conversion response, not a re-read
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 3.1: The generated document is editable and saves
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 4.1: A failed generation shows a distinct error, not a perpetual spinner
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 4.2: A conversion error keeps the text and offers retry
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 4.3: A transient poll error shows a distinct error, not a spinner
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 4.4: A generation that never finishes stops at the client deadline
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 5.1: Leaving with unsaved edits is guarded
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 6.1: A blank document can still be started from scratch
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo

### Scenario 6.2: The converted document appears in history
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo
