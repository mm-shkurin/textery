# Story 17: Export document to PDF / DOCX — Frontend Progress

Owns: Frontend Scenarios. Narrative/decisions/Spec checklist live in `progress.md`;
`ProductSpecification/stories.md` is the cross-file rollup.

This file tracks **which work units ran**.

## Frontend Scenarios (02_UI_Tests.md)

### Scenario 1.1: The editor offers a PDF and a DOCX export choice
- [x] red-selenium
- [x] red-frontend
- [x] green-frontend
- [S] red-frontend-api — control display is pure local toggle state; no backend call. The export endpoint (GET /documents/{id}/export) is exercised by scenario 5.1 (download).
- [S] green-frontend-api — same reason, no API for control display.
- [x] align-design — no mockup for story 17; styled to editor design tokens + defined the me-toolbar-row flex layout (breadcrumb left / export right). design-review PASS, coverage 100%.
- [x] green-selenium — real Chrome, 2 clean runs; against Vite dev :5273 + backend :8100 (no infra/.env on branch — run needs FRONTEND_PORT=5273 BACKEND_PORT=8100)
- [S] demo — autonomous loop, no interactive viewer; behavior already proven in real Chrome by green-selenium. Run `/demo TestExportControlDisplayAcceptance` manually to watch.

### Scenario 2.1: The export control is disabled while a request is in flight
- [x] red-selenium
- [x] red-frontend
- [x] green-frontend — real in-flight disabled state (not menu-close), released in `.finally()`; `exportDocument` throw-stub pending green-frontend-api. Addressed finding (1) by implementing a genuine disabled lock. Finding (2) release-path sibling test still owed — carry to a strengthening step (see below). **original findings on RED test (23ab6ce):** (1) *vacuous-pass risk (agent-review):* the RED test clicks the same captured `pdfOption` node twice; if green closes the menu on select, the 2nd click hits a detached node and passes with NO lock. Green must implement a real disabled/in-flight state on the control (per scenario title "control is disabled while a request is in flight") and the test must re-query + assert the control is disabled between clicks so the 2nd click reaches a live handler. (2) *lock-never-releases (premortem):* the never-settling mock can't prove release — add a sibling test with a manually-resolvable deferred asserting the control re-enables and a fresh click fires a 2nd `exportDocument` after settle (success AND reject paths; reject overlaps 3.2).
- [x] red-frontend (coverage: guard early-returns when locked or no documentId) — coverage-pinning: guard `if (isExporting || !documentId) return` already exists in prod (green-frontend). 2 tests (locked→call count stays 1 + payload `('doc-1','pdf')`; no-documentId→0 calls). No skip marker (tests pass green, lock the branch). Branches 83.33%→100%. Added `beforeEach(vi.clearAllMocks)` (no global clearMocks on project). test-review strengthened the surviving-call assertion to `toHaveBeenNthCalledWith`.
- [x] green-frontend (coverage: guard early-returns when locked or no documentId) — NO-OP: guard `if (isExporting || !documentId) return` (ExportControl.tsx:28) already shipped in original green-frontend; zero production change. 2/2 guard tests green, suite 132 pass. **Owed follow-ups (non-gating, from agent-review+premortem on dc7df3a):** (a) test 1 vacuously pins the `isExporting` branch — the option button's `disabled={isExporting}` masks the handler guard (jsdom skips onClick on disabled), so deleting `isExporting ||` from the handler leaves test 1 green. Needs a test reaching `handleExport` with isExporting=true bypassing the disabled attr. (b) release-path still uncovered (finding 2) — never-settling mocks never exercise `.finally(setIsExporting(false))`; owed resolvable-deferred sibling (resolve+reject, reject overlaps 3.2).
- [x] red-frontend-api
- [x] green-frontend-api — implemented via shared transport `responseType: 'blob'` (httpClient `performRequest` returns `res.blob()` AFTER the `res.ok` guard); `exportDocument` now `Promise<Blob>` through `send`/`authorizedRequest`, inheriting res.ok→HttpError, 401 renew+replay, and withTimeout. 508/508 pass. Owed non-ok case now COVERED: added an export-level guard test (premortem a227968) asserting exportDocument rejects on a non-ok response and never reads the blob — pins the res.ok-before-blob ordering so a future reorder goes red. 401-renew still relies on the shared transport's own tests (acceptable). Design directive (fulfilled): blob variant added to shared authorized transport, not a bare fetch; signature `Promise<Blob>`.
- [x] align-design — styled the in-flight disabled state (opacity 0.35 + not-allowed + neutralized disabled:hover) matching me-toolbar-btn:disabled. No mockup. design-review PASS.
- [x] red-selenium (determinism strengthening: throttle network + wait for in-flight before 2nd click) — hardened `manual_editor_export_control_statements.py` (128 lines): added `throttle_network`/`clear_network_throttle` (CDP `Network.emulateNetworkConditions`, `_SLOW_LATENCY_MS=2500`) + `wait_for_export_in_flight` (WebDriverWait on `_pdf_option_is_disabled` — proves `disabled`/`aria-disabled=true`, the real `disabled={isExporting}` lock, NOT 3.1's not-yet-existing indicator). `trigger_export_as_pdf_twice` now: throttle→open→click PDF→wait-in-flight→2nd click (dropped)→clear throttle. `==1` unchanged; requestWillBeSent counts the held-open first GET as 1. Test class stays `@pytest.mark.skip` (unskip+live run = green-selenium). Analytical RED: if lock absent, 2nd click hits enabled option→2nd GET→count 2→fails. test-review PASS 0 fixes.
- [x] red-selenium (fix: throttle AFTER opening the export control, not before) — reordered `trigger_export_as_pdf_twice` to open→resolve-option→throttle→click→wait-in-flight→2nd-click→clear. **Verified LIVE** (temp-unskip, real Chrome :8100/:5273): `1 passed in 11.39s`, `==1` held, and the premortem disabled-2nd-click landmine did NOT fire — real ChromeDriver dropped the click on the disabled button cleanly (no ElementNotInteractable/Intercepted/Stale). Skip marker re-added (formal unskip = green-selenium). test-review PASS 0 fixes. **green-selenium live run FAILED (a20e2db):** `trigger_export_as_pdf_twice` calls `throttle_network` at line 78 BEFORE `open_export_control` at line 79; under the 2500ms CDP throttle the `export-control-trigger` never becomes visible within the 5s wait → `TimeoutException` on the FIRST menu-open, before any click (NOT the premortem disabled-2nd-click landmine). Sibling 1.1 passes with the same trigger — only difference is 2.1 throttles first. Diagnosis: the export GET fires only on the PDF-option click, so the throttle must be active only for that click — move `throttle_network` to AFTER `open_export_control` (menu-open is a local toggle, no network). Fix: reorder to open→throttle→click→wait→2nd click→clear. Then green-selenium can retry.
- [x] green-selenium — removed `@pytest.mark.skip` from `TestExportControlInFlightAcceptance`; real Chrome (:8100/:5273), **2 clean runs** (9.57s, 9.89s), count==1 both. Version-dependent disabled-2nd-click landmine did NOT fire either run. Also dropped the now-dead `import pytest` (decorator was its only use). Scenario 2.1 acceptance now GREEN + enabled. Prior watch notes retained below for history. **premortem watch (247299e):** the 2nd `pdf_option.click()` lands on a now-disabled `<button>`; jsdom silently drops it but real ChromeDriver may raise `ElementNotInteractable`/`ElementClickIntercepted` (or stale), erroring for the wrong reason instead of proving the lock. If that happens at unskip, the green-selenium fix is to tolerate/assert the disabled 2nd click explicitly (Statements change → belongs to a red-selenium follow-up, not remove-marker-only) — STOP and report rather than forcing it. throttle-leak/timeout/preflight all dismissed (function-scoped driver quits; disabled set synchronously; GET-only count excludes OPTIONS). **premortem 429b5e3 (2nd verified-live pass, still non-gating):** the disabled-2nd-click drop is ChromeDriver-version-dependent and was proven exactly once — RUN green-selenium TWICE (matching 1.1's 2-run convention) to shake out the flake; if either run errors on the 2nd click (`ElementNotInteractable`/`StaleElementReference`), the red-selenium follow-up is to wrap+re-fetch the 2nd click so the drop is asserted by the test, not left to undefined driver behavior. Optional belt-and-suspenders: fold `clear_network_throttle` into a `finally` (currently skipped if the 2nd click raises; blast radius is 1 test since the driver is function-scoped).
- [S] demo — autonomous loop, no interactive viewer; behavior already proven in real Chrome by green-selenium (2 clean runs). Run `/demo TestExportControlInFlightAcceptance` manually to watch.

### Scenario 3.1: An in-flight export shows a progress state
- [x] red-selenium — added `EXPORT_SPINNER=[data-testid='export-spinner']` + `trigger_throttled_pdf_export` + `assert_exporting_indicator_is_shown` (WebDriverWait on visibility, throttle holds GET /export open, open-before-throttle ordering) to `manual_editor_export_control_statements.py` (155 lines). New skipped class `TestExportControlProgressAcceptance`. Analytical RED: indicator not rendered yet → live run would TimeoutException on the visibility wait; green-frontend adds the spinner. test-review PASS 0 fixes. Live run deferred to green-selenium.
- [x] red-frontend — new `ExportControl.progress.test.tsx` (67 lines, `describe.skip`): 3 tests pinning `export-spinner` edges via manually-resolvable deferred — (1) `queryByTestId` null when idle, (2) `getByTestId` visible while in-flight, (3) `waitForElementToBeRemoved` after resolve (success path; reject=3.2). RED confirmed: 2 failed (rising+falling edge, TestingLibraryElementError "Unable to find [data-testid=export-spinner]") + 1 passed (idle-absent), matched prediction exactly. Closes the convergent rising-edge-only gap. test-review PASS 0 fixes.
- [x] green-frontend — added inline `{isExporting && <span data-testid="export-spinner" className="me-export-spinner" aria-hidden />}` as a `.me-export-control` descendant (right after trigger), + `.me-export-spinner`/`me-export-spin` keyframes in ExportControl.css mirroring the `.me-save-spinner` precedent. No new state — reuses `isExporting` (cleared on resolve AND reject via existing `.finally`), so error-path removal is free before 3.2. Removed `describe.skip`. Progress file 3/3 pass; ExportControl 5/5; generation suite 176/176, 0 regressions. ExportControl.tsx 82 lines, .css 96 lines. Followed premortem guidance (inline not portal → jsdom+selenium agree). 2.1 disabled-lock untouched. **Review passes on de21f55 were cut off by a session limit (non-gating, commit stands); refactor was a no-op.** **a11y follow-up (partial agent-review probe):** the spinner is `aria-hidden="true"` with no text, so a screen-reader user gets NO feedback that an export is in progress — for a "progress state" scenario consider `role="status"`/`aria-live="polite"` with an sr-only label, and a `prefers-reduced-motion` guard on the spin keyframes (premortem candidate). Deferred — not required for the minimal green; raise as a polish/red step if desired.
- [S] red-frontend-api — UI scenario 3.1 (progress indicator) is pure client-side state: the spinner rides the existing `exportDocument` in-flight window (gated on `isExporting`), no new frontend-api surface. The export transport is already covered by 2.1's green-frontend-api. (API §3.1 in 01_API_Tests is a backend filename-encoding concern, not this UI layer.) Same rationale as 1.1.
- [S] green-frontend-api — same reason; no API for the progress indicator.
- [x] align-design — no CSS change needed: `.me-export-spinner` already matches the `.me-save-spinner` precedent exactly (14px, 2px border `rgba(17,18,20,0.35)`, `border-top-color:var(--btn-primary-fg)`, 0.7s linear) + editor tokens; only `align-self:center` added (correct for the inline-flex row) and a namespaced `me-export-spin` keyframe. No mockup for story 17. design-review PASS (no placeholder data, no divergent magic values). Coverage: ExportControl.tsx 100% statements/branches/lines — new spinner branch covered both edges; lone uncovered fn is the `.catch(()=>{})` rejection handler, correctly deferred to 3.2.
- [x] green-selenium — removed `@pytest.mark.skip` from `TestExportControlProgressAcceptance` (and the now-dead `import pytest`, its sole user). Real Chrome (:8100/:5273), **2 clean runs** (9.70s, 7.30s), spinner visible in-flight both. No flake/landmine, no chromedriver orphans. Spinner already shipped in green-frontend 3.1 (de21f55), so acceptance passed as-is on unskip.
- [S] demo — autonomous loop, no interactive viewer; behavior already proven in real Chrome by green-selenium (2 clean runs). Run `/demo TestExportControlProgressAcceptance` manually to watch.

### Scenario 3.2: An export error is shown with retry, document unchanged
- [x] red-selenium — new skipped class `TestExportControlErrorAcceptance` + `ExportErrorStatementsMixin` (CDP `Network.setBlockedURLs` on `*/export*` for a deterministic failed GET; `trigger_failed_pdf_export`, `assert_export_error_with_retry_is_shown`, `assert_document_view_is_unchanged`). Split shared `export_control_locators.py` (avoid circular import) — all files <200. Analytical RED (prod `.catch(()=>{})` swallows the reject at ExportControl.tsx:34, no `export-error`/`export-retry` node exists): predicted `TimeoutException` on the `export-error` visibility wait, matched. test-review 3 strict fixes: pinned error text `"Не удалось экспортировать документ"`, retry label `"Повторить"`, and a real content-baseline unchanged assertion (captured before the blocked GET). green-selenium performs the live remove-marker run.
- [x] red-frontend — new `ExportControl.error.test.tsx` (5 tests, deferred resolve/reject mirroring 3.1). Pins: idle=no error, success=no error, reject→`export-error` visible with exact transport text `"Не удалось экспортировать документ"`, `export-retry` visible accessible-name `"Повторить"`, and **retry re-dispatch** — click fires 2nd `exportDocument` (`toHaveBeenCalledTimes(2)`+`toHaveBeenNthCalledWith(2,'doc-1','pdf')`) then clears error. Closes the premortem retry-as-decoration gap from red-selenium. RED run: **3 failed** (tests 3,4,5 — `TestingLibraryElementError` on missing `export-error`/`export-retry`, prod swallows reject at ExportControl.tsx:34) **| 2 passed** (idle/success guards), matched prediction field-by-field. test-review PASS 0 fixes (assertions already strict: `toHaveTextContent`/`toHaveAccessibleName`/`toHaveBeenNthCalledWith`).
- [x] green-frontend — added `error` + `lastFormat` state to ExportControl.tsx. `handleExport` captures `lastFormat`, clears error at entry, and on catch sets `err.message` (surfaces transport text, not a literal); error cleared **only on success** so it survives the retry in-flight window. `handleRetry` re-dispatches `handleExport(lastFormat)` — same id+format, reusing `isExporting` lock (no double-dispatch). Inline `export-error` (role=alert) + `export-retry` button (`aria-label="Повторить"`). Premortem guards folded: same-format retry (via `lastFormat`), error-re-raised-if-retry-also-rejects (clear-on-success-only), rejection handled (no unhandled promise). 5/5 error test green; full component suite **140/140**, no 1.1/2.1/3.1 regression. ExportControl.tsx 115 lines, .css 121 (both <200). Minimal styling; pixel alignment deferred to align-design. **CORRECTION to original claim:** the behavior commit (119bce9) message says "clear the error at handleExport entry" — the code does NOT (and must not: entry-clear would drop the banner on retry dispatch, breaking test 5's "error visible during retry in-flight"). Error clears only on success. **Owed follow-ups (non-gating, agent-review+premortem on 119bce9):** (a) fresh (non-retry) export after a failure shows the stale banner + in-flight spinner together for the new export's in-flight window — clear the error only for *fresh* dispatches, not retry; no test guards this. (b) retry mis-targets when `documentId` prop changes/goes null while banner shown — `handleExport` reads current-render `documentId`, not the failed one; guards test proves null-mid-mount is a live pattern. Reset `error`/`lastFormat` on documentId change. (c) raw non-localized transport strings (e.g. "Failed to fetch" from `describeFailure`) render verbatim in the `role=alert`; tests only reject with the clean localized string. Bound/localize the displayed message. All three warrant a 3.2 strengthening red-frontend step or align-design pass.
- [S] red-frontend-api — UI-3.2 (error + retry) surfaces the EXISTING `exportDocument` reject; no new frontend-api surface. The transport's non-ok/reject path is already covered by `documentApi.export.test.ts` (added in 2.1 green-frontend-api: exportDocument rejects on a non-ok response, res.ok-before-blob ordering). Retry just re-invokes the same `send`/`authorizedRequest` chain. (API §3.2 in 01_API_Tests is a backend default-filename concern, not this UI layer.) Same rationale as 3.1.
- [S] green-frontend-api — same reason; no API for the error/retry UI. Reject path + 401-renew inherited from the shared authorized transport, covered by its own tests.
- [x] align-design — no mockup for story 17; aligned the error/retry surface to editor tokens. Fixed the one divergence: `.me-export-error` used a divergent `var(--text-danger, #d9534f)` (wrong token name + off-palette hex fallback) → replaced with the shared `var(--error)` (#ef4444, index.css:53) matching every sibling error surface (auth, save-status, chat). `.me-export-retry` already used shared tokens (`--border-subtle`/`--bg-surface`/`--text-primary`/`--bg-card-muted`). design-review PASS (no placeholder data — error text is dynamic `err.message`; no remaining magic values on the 3.2 surface; out-of-scope pre-existing spinner/menu rgba() flagged for awareness only). Coverage CLEAN, no regression: ExportControl.tsx 100% stmts/lines/funcs, branch 85.71% — lone uncovered branch is the defensive non-Error `String(err)` side at L48 (optional coverage follow-up; ties to the premortem localize-message note). CSS-only unit, no TSX change. **Owed follow-up (non-gating, premortem on a016311):** WCAG AA contrast — `--error` #ef4444 on white `--bg-surface` is ~3.76:1, below the 4.5:1 AA floor for 13px normal text. This is a **project-wide** token property shared by every error surface (auth/save-status/chat), not unique to export — belongs to a design-token a11y pass (darken `--error` or enlarge/bolden error text), NOT a revert of this alignment. align-design verify-only PASS (only `var(--error)` remains, no divergence).
- [x] red-frontend (fix: export-error shows a fixed localized message, not the raw transport string) — **DISCOVERED by green-selenium failure (see below).** green-selenium ran the real transport: CDP `setBlockedURLs(["*/export*"])` makes the export GET reject at the network layer with the browser's raw `"Failed to fetch"`, and `ExportControl` surfaces the caught `err.message` verbatim, so the banner read `"Failed to fetch"` — but the acceptance Statements pin the localized `"Не удалось экспортировать документ"` → `1 failed in 19.85s` (AssertionError on the exact-text check; the element/role/retry wiring is all correct, only the text is wrong). The green-frontend unit test passed only because it mocked an unrealistic rejection carrying the localized text (`new Error("Не удалось экспортировать документ")`); the real transport (`send`/`describeFailure`) returns the raw message. This is premortem incident #2 (raw non-localized string leak, logged on 119bce9) confirmed live. Red step: add a unit test rejecting with `new Error("Failed to fetch")` (and a non-Error reject — closes the L48 `String(err)` branch gap too) asserting `export-error` STILL shows the fixed localized string. Marker on `TestExportControlErrorAcceptance` restored to skip (green-agent's removal reverted, uncommitted); this scenario's green-selenium re-runs after the fix. **Done:** added 2 tests to `ExportControl.error.test.tsx` — (1) reject `new Error("Failed to fetch")` → assert `export-error` shows exactly `"Не удалось экспортировать документ"` AND `not.toHaveTextContent("Failed to fetch")`; (2) reject non-Error `'boom'` → same localized text, `not.toHaveTextContent("boom")` (closes L48 `String(err)` branch). RED run **2 failed** (DOM showed `Failed to fetchПовторить` / `boomПовторить`) **| 5 passed**, matched prediction field-by-field. Root: `describeFailure` (send.ts:43) returns raw `error.message` on a network-layer failure. test-review PASS 0 fixes (negative guards defeat concatenation false-pass; file 147 lines).
- [x] green-frontend (fix: render the fixed localized export-failure message, not `err.message`) — **DONE:** added component constant `EXPORT_ERROR_MESSAGE = 'Не удалось экспортировать документ'`; `.catch` now `setError(EXPORT_ERROR_MESSAGE)` (was `err instanceof Error ? err.message : String(err)`). Scoped to ExportControl.tsx only (no send.ts/documentApi.ts change). Error suite 7/7, full frontend 521/521, 0 regressions. File 120 lines. **direction (agent-review+premortem on 3dd4710):** take the SCOPED component-constant fix (ExportControl shows a fixed localized `"Не удалось экспортировать документ"` on any export failure) — low blast radius, makes the 2 new unit tests + green-selenium green. Do NOT change shared `send.ts`/`describeFailure` autonomously. **Owed follow-ups (non-gating, need user sign-off — shared-infra / UX):** (a) ROOT cause is `describeFailure` (send.ts:43) returning raw `error.message` on a network-layer failure — the identical leak hits create/save/load too; a transport-level fix (non-HttpError branch → `fallback`) would cure all four callers at once. Scoped component fix leaves the other three leaking. (b) the blanket constant flattens `describeFailure`'s HttpError arm too, discarding a genuinely useful localized server `message` (e.g. "Документ слишком большой") + the `(HTTP <status>)` breadcrumb. Add an HttpError-carrying-server-message test and decide: constant only on the network branch vs constant always. (c) literal `"Не удалось экспортировать документ"` will be duplicated between the component and documentApi.ts:139 — extract one shared constant to avoid drift. (d) `SessionExpiredError` mid-export is caught into the export banner instead of the global signed-out flow (pre-existing, ExportControl `.catch`).
- [x] red-frontend (fix: `export-error` element text excludes the nested retry label) — **DISCOVERED by green-selenium failure.** After the localized-message fix (dd8e5d0) the banner text is correct, but the acceptance still fails deterministically: the Statements pin `assert error.text.strip() == "Не удалось экспортировать документ"` reads the WHOLE `export-error` element, and the `export-retry` `<button>` is a **child** of that element (ExportControl.tsx:87–98), so Selenium's `.text` returns `"Не удалось экспортировать документ\nПовторить"` → `AssertionError` at `manual_editor_export_error_statements.py:96`. (Run 1 also hit the documented version-dependent render-timeout flake; run 2 was the deterministic text mismatch. 0/2 passed.) The unit tests missed it because Testing-Library `toHaveTextContent`/`toHaveAccessibleName` match substrings, not exact whole-node text. Red step: add a unit test asserting the `export-error` node's OWN text is exactly `"Не удалось экспортировать документ"` (e.g. `export-error` has no descendant text — retry label lives in a sibling), so a retry button nested inside the message node goes RED. Predicted RED: current DOM concatenates message+label in one node.
- [x] green-frontend (fix: separate `export-retry` from the `export-error` message node) — **DONE:** wrapped message + retry in `<div class="me-export-error-bar">`; `export-error` node now holds only `{error}` text, `export-retry` is a sibling. `role="alert"` kept on the message node (premortem guard), `aria-label="Повторить"` on the button, retry wiring/state/constant untouched. Moved the flex layout from `.me-export-error` to `.me-export-error-bar` in CSS. Un-skipped the target test. Error suite 8/8, generation 184/184, 0 regressions. tsx 122 / css 125 lines. **Owed follow-up (non-gating, premortem 548d370):** role-guard assertion still not added (green tests read-only) — a `getByRole('alert')` on `export-error` after failure is owed as a tiny red-frontend step so a future drop of `role="alert"` goes RED. so `export-error` contains ONLY the message text and `export-retry` is a **sibling** (wrap both in an outer container, or give the message its own `data-testid="export-error"` node). No behavior change beyond DOM structure; keep `role="alert"` on the message, `aria-label="Повторить"` on the button. Scoped to ExportControl.tsx (+ .css if layout needs a flex wrapper). **premortem follow-up (548d370, non-gating):** the restructure must NOT drop `role="alert"` off the message node — no test guards the alert role today (only `textContent` + retry visibility). green-frontend should keep `role="alert"` on the `export-error` message span AND add a `getByRole('alert')`/`toHaveAttribute('role','alert')` assertion so a lost announce goes RED.
- [x] green-selenium — removed `@pytest.mark.skip` from `TestExportControlErrorAcceptance` (+ the now-unused `import pytest`, its sole user). Real headless Chrome (:8100/:5273), **2 clean runs** (20.65s, 11.88s), 1 passed each. `export-error`.text exactly `"Не удалось экспортировать документ"` — the DOM-sibling fix (242cabb) resolved the nested-retry text leak. Version-dependent render-timeout flake did NOT fire either run. Scenario 3.2 acceptance now GREEN + enabled. **Owed non-gating follow-up carried forward:** role-guard unit assertion (`getByRole('alert')` on `export-error`) still owed as a tiny red-frontend step (premortem 548d370/242cabb) — `role="alert"` is preserved in prod but unguarded by any test. **Additional premortem follow-ups (ab6f4cc, non-gating):** (i) the acceptance visibility waits use a tight 5s `WAIT_TIMEOUT_SECONDS`; the documented render-timeout flake could re-fire under CI contention with a different chromedriver — consider an explicit "export control ready" wait before the failing click, or a rerun/quarantine policy. (ii) acceptance asserts retry is only *visible* (not clicked) — retry re-dispatch is covered at unit level (`ExportControl.error.test.tsx`) but not at acceptance; an inert retry button would ship green at the acceptance layer.
- [S] demo — autonomous loop, no interactive viewer; behavior proven in real Chrome by green-selenium (2 clean runs). Run `/demo TestExportControlErrorAcceptance` manually to watch.

### Scenario 4.1: Exporting with unsaved edits saves or warns first
**Design decision (pinned): SAVE-FIRST.** Export renders the *stored* HTML, so unsaved edits would ship a stale file. Chose the spec's first branch — on export with unsaved edits, the app saves first (PUT /documents/{id}) and only then fires the export GET — reusing the existing `useDocumentSave`/`save()` machinery (no new modal/warn UI). ExportControl today gets only `documentId` (ManualEditor.tsx:164); green threads `hasUnsavedChanges` + `save` in. Alternative (explicit warn dialog) was available per spec but rejected: higher-risk new UI, worse flow.
- [x] red-selenium — new `@pytest.mark.skip` class `TestExportControlDirtyExportAcceptance` + `ExportDirtyStatementsMixin` (`manual_editor_export_dirty_statements.py`, 121 lines; mixed into `ExportControlStatements`, 163). Dirties the editor, captures CDP perf-log, triggers PDF export, asserts **save-first ordering by position**: PUT (save) present AND `save_index < export_index` (GET /export). Analytical RED (live deferred to green-selenium): current wiring fires export GET with NO preceding PUT → `save_index is None` → AssertionError "no save request was observed". test-review PASS 0 fixes (strict position+presence, can't false-pass on missed PUT). Files 121/163/103 <200.
- [x] red-frontend — new `ExportControl.dirty.test.tsx` (`describe.skip`, 2 tests) pinning SAVE-FIRST at component level. (1) dirty path: `hasUnsavedChanges`+deferred `save` → export triggered → `save` called, `exportDocument` NOT yet (never-settling export mock), then after `resolveSave()` export dispatches `('doc-1','pdf')` — proves ordering structurally. (2) clean path (`hasUnsavedChanges=false`): `save` never called, `exportDocument` fires directly. RED: **1 failed** (`expect(save).toHaveBeenCalledTimes(1)` got 0, ExportControl.dirty.test.tsx:39 — component ignores unknown `save` prop) **| 1 passed** (clean guard), matched prediction field-by-field. TS will flag extra props until green threads `hasUnsavedChanges`+`save` into `ExportControlProps` (both already in scope at ManualEditor.tsx:164). `save` threaded as prop; `exportDocument` stays mocked module import (existing pattern). test-review PASS 0 fixes (ordering pinned via rising/falling deferred edges, exact `toHaveBeenNthCalledWith`).
- [x] green-frontend — added optional props `hasUnsavedChanges?: boolean` + `save?: () => void | Promise<void>` to ExportControl. `handleExport` sets `isExporting` lock FIRST (double-click guard spans the save window), then async `run()`: if dirty, `await save()` (only when it returns a thenable — keeps void-save path synchronous) BEFORE `await exportDocument`; a rejecting/throwing save SKIPS the export (stale-file guard, premortem #1 folded); `.catch` sets fixed banner, `.finally` releases lock (premortem #2 folded). Un-skipped ExportControl.dirty.test.tsx (only allowed test change) — 2/2 green. Full suite **524 passed / 0 failed**. **ManualEditor wiring deliberately NOT landed here:** threading `hasUnsavedChanges`/`save` into `<ExportControl>` regresses scenario 2.1's `ManualEditor.exportInFlight.test.tsx` (its `saveDocument` mock returns `undefined`, not a promise, so save-first throws sync and export never dispatches). That mock fix is a test change → RED-phase; deferred to the discovered wiring step below (must land before green-selenium 4.1). ManualEditor.tsx left at `documentId`-only.
- [S] red-frontend-api — save-first reuses EXISTING transport: save = PUT (covered by save scenarios' transport tests), export = GET (covered by 2.1 green-frontend-api). No new frontend-api surface. Same rationale as prior scenarios' api skips.
- [S] green-frontend-api — same reason; no API for the save-first ordering.
- [~] red-frontend (wire ManualEditor save-first + fix 2.1 exportInFlight mock) — **DISCOVERED by green-frontend.** green-selenium 4.1 needs ManualEditor to thread `hasUnsavedChanges`/`save` into `<ExportControl>`, but doing so regresses `ManualEditor.exportInFlight.test.tsx` (2.1): mock `saveDocument` returns `undefined` → `undefined.then()` throws sync → export never fires → its synchronous single-dispatch assertion breaks. Fix (RED): update that test's mock to `mockResolvedValue(<persisted doc shape>)` and assert export via `await waitFor(() => exportDocument called once)`; thread the props in ManualEditor.tsx. Predicted RED until wiring lands. **CRITICAL direction (agent-review+premortem on 47c1b11 — both flagged same gap):** the real `useDocumentSave.save` is typed/implemented `() => void` (useDocumentSave.ts:54,139-146) — fire-and-forget, returns `undefined`. ExportControl's thenable-guard (`if (saving && typeof saving.then === 'function') await saving`) therefore does NOT wait for a void save → export races the in-flight PUT → ships STALE file (the exact 4.1 bug), and a failed save (swallowed by useDocumentSave's own `.catch` at :109, never rejects) still exports. The wiring step MUST make save awaitable+rejectable: change `useDocumentSave.save` to return `Promise<void>` that rejects on failure (or require a `Promise<void>` save in ExportControlProps), and add unit tests for (a) void/non-promise save path and (b) rejecting-save-skips-export. Also decide save-failure banner semantics: a save-layer failure (SessionExpiredError/VersionConflictError have data-loss-specific wording) must NOT be masked by the generic export banner, and must not double-banner with ManualEditor's saveError. **Done:** new `ManualEditor.dirtyExport.test.tsx` (`it.skip`) pins save-first ORDERING at ManualEditor level — save mock pushes `'save'` into order array at RESOLUTION time (not invocation), so `callOrder` must equal `['save','export']`; a fire-and-forget void save resolves later → `['export','save']` → RED. Exact args pinned (`saveDocument('doc-1','unsaved edit',7)`, `exportDocument('doc-1','pdf')`), types real unsaved text so stale-ship bug is genuinely exercised. RED: **saveDocument called 0×** (ManualEditor.tsx:164 renders ExportControl w/o save prop) — matched prediction. Also fixed 2.1 `ManualEditor.exportInFlight.test.tsx`: `saveDocument` mock → `mockResolvedValue`, assertion → `await waitFor(exportDocument called once)` (intent preserved: 1 export on double-click); stays GREEN now (save not yet threaded on that path). test-review strengthened both with exact-arg assertions.
- [x] green-frontend (wire ManualEditor save-first) — thread `hasUnsavedChanges`+`save` into `<ExportControl>` at ManualEditor.tsx:164 AND make `useDocumentSave.save` return a rejectable `Promise<void>` (per critical direction above); un-skip dirtyExport; make it + full suite green. **premortem gaps to guard (f145277, 3 incidents):** (A, SEVERE) `performSave.catch` (useDocumentSave.ts:109-122) SWALLOWS errors — a naive promise-wrap that leaves the catch intact RESOLVES on failure, so `ExportControl`'s `await save()` completes and STILL exports the stale stored HTML. Green MUST make the returned promise REJECT on save failure (propagate/rethrow from the catch, or wrap so failure rejects) — otherwise reject-skips-export is dead. Owed guard: ManualEditor-level test, `saveDocument` rejects → `exportDocument` called 0× + save banner shown. (B) `save()` early-returns when a save is already in-flight (useDocumentSave.ts:141-144) setting `saveAgainRequested`; the returned promise must settle only AFTER the in-flight + any queued re-save complete, else export races the PUT (stale via 2nd door). (C) banner semantics: a rejecting save sets BOTH `ExportControl` generic `EXPORT_ERROR_MESSAGE` AND `useDocumentSave` `saveError` (session-expiry/version-conflict data-loss wording, ManualEditor.tsx:181) — decide which surfaces; the data-loss message must NOT be masked and must not double-banner. If B/C are too large for this green, split them to a discovered red/green pair rather than shipping A unguarded. **DONE:** (1) `useDocumentSave.save` now `() => Promise<void>` — `performSave` returns a promise via `.then(onFulfilled, onRejected)` (not `.then().catch()`) so the rejection handler owns only THIS save's failure; the queued re-save is `return`ed from the fulfilled handler so the chain settles only after it completes (B); rejected handler keeps all side effects (console.error, reset refs, `setSaveError`) then RETHROWS so the awaiting caller skips a stale export (A). Added `inFlightRef` so the in-flight early-return returns the LIVE chain, not an already-resolved promise (B). (2) ManualEditor.tsx:164 threads `hasUnsavedChanges`+`save`; Save button wraps `save()` in `.catch(()=>{})` (banner already set before rethrow, avoids unhandled rejection). (3) ExportControl: a `save()` rejection on the dirty path is caught locally and `return`s (export skipped, lock released by `finally`) WITHOUT setting the generic export banner — so the accurate SessionExpired/VersionConflict save banner is not masked (C); export-path rejections still show generic banner. Un-skipped dirtyExport (only test change). Full suite **525 passed / 0 failed / 0 skipped**, tsc clean. Files: useDocumentSave 167, ExportControl 153, ManualEditor **200 (at limit — refactor watch)**. **Owed test guards (production shipped, tests not yet — discovered red steps):** A: ManualEditor-level test `saveDocument` rejects → `exportDocument` 0× + save banner shown; C: banner-not-masked assertion on save-reject during dirty export.
- [x] align-design — **no-visual-change unit.** Scenario 4.1 is pure save-first ordering + banner-suppression logic; added NO renderable element and NO ExportControl.css change (diff confirmed empty). design-review PASS: every string literal on the 4.1 surface (EXPORT_ERROR_MESSAGE, SAVE_ERROR_MESSAGE, CONFLICT_ERROR_MESSAGE, `'Экспорт'`/`'Повторить'`/`pdf`/`docx`) is legitimate localized UI copy, not placeholder data; ManualEditor renders no hardcoded content (all state/prop/hook-driven). Coverage (`--focus`, 187 pass): useManualEditorInstance 100/100/100, ManualEditor 100% branches, ExportControl 90.5% branches, useDocumentSave 88% branches — the 3 gaps map EXACTLY to the owed A/B/C guard step (see below). No CSS/TSX change → refactor + verify no-ops, progress-only commit.
- [~] red-frontend (guard the save-first failure contracts A/B/C) — **DISCOVERED by agent-review+premortem on 596fd1c (both converge). Exact uncovered lines pinned by coverage --focus:** (C) `ExportControl.tsx:74` `catch { return }` — dirty export, promise-`save()` REJECTS → skip export, no generic banner. (A) `useDocumentSave.ts:155` public `save()` null-guard true-branch (call before init sets documentId/editor). (B) `useDocumentSave.ts:160` `return inFlightRef.current ?? Promise.resolve()` — concurrent `save()` while one runs returns the LIVE chain (both the truthy return and the `?? ` fallback uncovered). **Exclude** `useDocumentSave.ts:85` (performSave null-guard) — unreachable via current call graph (already gated by L155); do NOT reach into internal performSave to cover it (refactor may mark dead).** The shipped save-first LOGIC is correct but the guards that are scenario 4.1's whole reason for existing are UNTESTED — a future refactor re-introduces bug 4.1 while all 525 tests stay green. Add guard tests (pass on current code, lock the branches — same coverage-pinning pattern as 2.1's guard red steps): (A) ManualEditor dirty export where `saveDocument` REJECTS → assert `exportDocument` called **0×** (stale file not shipped). (C) same reject → assert generic `export-error` banner is NOT present AND the accurate save banner (SessionExpired/VersionConflict) IS — banner not masked. (B) `useDocumentSave` has NO unit test file; add one: call `save()` twice (2nd while in-flight) → returned promise settles only AFTER the queued re-save's `saveDocument` resolves; and its rejection propagates. These pin `useDocumentSave.ts:114` (return performSave re-save), `:133` rethrow, and `ExportControl.tsx:64-76` (catch→return, banner skip).
- [ ] green-frontend (guard A/B/C) — NO-OP expected (production already shipped in green-frontend wiring, 596fd1c); un-skip/confirm the guard tests green, zero production change.
- [ ] green-selenium
- [ ] demo

### Scenario 5.1: A successful export delivers a downloaded file
- [ ] red-selenium
- [ ] red-frontend
- [ ] green-frontend
- [ ] red-frontend-api
- [ ] green-frontend-api
- [ ] align-design
- [ ] green-selenium
- [ ] demo
