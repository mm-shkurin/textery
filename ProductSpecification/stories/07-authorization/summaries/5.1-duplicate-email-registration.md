## red-selenium (2026-07-15)

**Quirk:** Story 7's backend layer has not started — `POST /api/v1/auth/register` returns 404 (verified live via curl against the local backend container). stories.md's Backend column for Story 7 is still `·`.
**Where:** Any scenario whose Selenium test needs a real auth endpoint (5.1 onward; also retroactively broke 3.1, since its RegisterForm now calls the real API after 5.1's green-frontend).
**Impact:** Frontend-only sessions cannot green these Selenium steps until the backend session lands the endpoint — this is a genuine cross-layer block, not an infra/docker artifact (that separate docker-dist blocker was fixed this session).
