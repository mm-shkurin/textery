## green-selenium (2026-07-15)

**Surprise:** The test docstring's assumption ("no real registration API call is wired in yet") is now stale — Scenario 5.1's green-frontend replaced RegisterForm's `useSubmitPlaceholder` with a real `registerApi.register` call, so the in-flight window this test observes is now driven by a real (currently 404ing) fetch, not the 500ms setTimeout placeholder.
**Why:** Scenario 5.1 landed after this test was written; only RegisterForm was migrated off the placeholder (LoginForm/VerifyCodeForm still use it).
**Impact:** This test's in-flight observation window is now bounded by real network latency (or immediate rejection on 404) instead of a guaranteed 500ms — may need re-verification once the backend endpoint exists.

## green-selenium (2026-07-15)

**Quirk:** `acceptance/conftest.py`'s `app_url` fixture reads `FRONTEND_PORT` from the process environment (default 5173), not from `infra/.env` directly.
**Where:** `acceptance/conftest.py:38-39`.
**Implication:** Running `pytest` for frontend acceptance tests requires `export FRONTEND_PORT=80` (or whatever `infra/.env` currently sets) in the same shell first, or the fixture silently targets the wrong port and Selenium gets `net::ERR_CONNECTION_REFUSED`.
