# Scenario 2.1 — a never-configured document reads as unconfigured

## red-adapter rest (2026-08-05)

**Quirk:** The RED skip markers guarding the four-row title contract are spelled two ways — a
shared `_RED_TITLE_INTENT` constant at three sites, and an inline literal at the fourth — so
`grep -rn _RED_TITLE_INTENT` enumerates exactly the three whose removal leaves the contract dark.
**Where:** `backend/adapters/rest/tests/router/document/test_save_document_router.py:20,69,97,134`
and `test_save_document_title_router.py:8`.
**Implication:** A correct green that lifts only the greppable markers reports 92 passed, 5 skipped,
0 failed — a clean run is not evidence the contract is live; only the skip count is.

## red-adapter rest (2026-08-05)

**Quirk:** This worktree has no `backend/.venv` and no Postgres listening, so `pytest backend/`
runs under system Python 3.12 and silently skips the 62 db-adapter tests plus 3 rendering import
guards.
**Where:** `backend/adapters/db/tests/`, `backend/adapters/rendering/tests/`.
**Implication:** Any "N passed" figure recorded here is scope-dependent — the same commit reads as
598/11 or 641/73 depending on what was collected, and the two were conflated once already.
