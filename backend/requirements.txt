fastapi==0.139.0
uvicorn==0.34.0
httpx==0.28.1
sqlalchemy==2.0.49
asyncpg==0.31.0
bcrypt==5.0.0
PyJWT==2.13.0
# HTML sanitizer for manual-document content (story 5). Rust/ammonia binding.
# bleach is archived/EOL since Jan 2023 and its own README points here.
nh3==0.3.6
alembic==1.18.4
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-mock==3.15.1
pytest-cov==7.1.0
# Linter and formatter, configured in pyproject.toml. Pinned exactly, like
# everything else here: ruff ships new rules in patch releases, so an unpinned
# version turns "CI went red" into a question about which ruff the runner drew
# rather than about the commit.
ruff==0.15.22
# Static type checker, configured in pyproject.toml. Pinned for the same reason
# ruff is: mypy tightens inference in minor releases, so an unpinned version
# turns "CI went red" into a question about which mypy the runner drew.
mypy==1.19.0
