# Технический долг Textery — срез на 20.07.2026

Канонический реестр с полным контекстом «что/почему/где всплывёт» —
`.memory-bank/tasks/known-debt.md`. Этот файл — **краткий срез актуально-открытого**
на 20.07, отсортированный по важности, плюс находки ре-аудита 20.07. Не дублирует
реестр — ссылается на него.

## Высокий приоритет (безопасность / целостность данных)

| # | Долг | Где всплывёт | Источник |
|---|---|---|---|
| Story 7 rejection-ветки | `VerifyAccount`: `find_by_email`/`find_active_by_account_id` возвращают `Optional`, но местами разыменовываются безусловно — неизвестный email / неверный / просроченный / повторный код сейчас дают 500 вместо чистого отказа | сценарии 3.2–3.6 story 7 | `07-authorization/progress.md` |
| Story 2 (#2) userId-ретрофит | Story 1 строила `Generation`/`Document` анонимно. Task 4 добавил `owner_id` генерациям и закрыл анонимный `POST`/cross-account read. Убедиться, что ВСЕ пути документов/генераций теперь требуют идентичность | продолжение story 7 / любая новая история с данными пользователя | known-debt #2 (частично закрыт task 4) |
| Валидация Cc/Cs (#8) | `_is_blank_topic()` фильтрует только Cf, но не Cc (NUL, BEL) и Cs (одиночные суррогаты) — topic из одних управляющих символов проходит как «непустой», ломается ниже по потоку | scenario 2.1 story 1 | known-debt #8 |

## Средний приоритет (архитектура / нереализованный план)

| # | Долг | Суть | Источник |
|---|---|---|---|
| Очередь задач (#6, #13) | Заявлен `arq`+Redis, реально `BackgroundTasks`+DB-sweep. Redis провизионится, но кодом не используется (задел). `technology.md` уже описывает реальный механизм честно | known-debt #6, #13 |
| Стриминг генерации (#5) | UI показывает простой loader, не token-by-token. Реальный SSE/WebSocket-стриминг — отдельный спринт | known-debt #5 |
| Форма генерации (#9) | Одно chat-поле маппится в `topic`; `volume`/`requirements`/`extra_wishes` — фиксированные дефолты. `02_UI_Tests.md` секция 4 + mockup 04 намеренно устарели. Scenario 7.2 (out-of-range volume) сейчас нетестируем | known-debt #9 |
| Backfill evening-demo (#10) | Вертикальный срез story 1 построен вне TDD-цикла; Task 3 добавил характеризацию постфактум. Сверять `[x]` чекбоксы с реальным поведением, не доверять слепо | known-debt #10 |
| Deploy вручную / IaC (#3) | Нет Terraform/облака; deploy `infra/docker-compose.yml` руками на существующий сервер. `terraform-yandex` агент установлен, но dormant | known-debt #3 |
| Kafka/Saga/idempotency (#7) | Кластер заметок тех-лида для billing (story 8) — не решён, поднять на `/interview` story 8 | known-debt #7 |

## Находки ре-аудита 20.07 (процессные / документационные)

| Долг | Суть | Действие |
|---|---|---|
| Story 5 `progress-backend.md` отсутствует | Обещан в `progress.md`, но не создан — backend/integration/security-сценарии story 5 нигде не трекаются. Пометка добавлена | Забутстрапить файл из `tests/{01_API,05_Security,06_Integration}_Tests.md`, когда стартует backend story 5 |
| `stories.md` Story 5 маркеры `Back`/`Sec` = 🔧 | Опережают реальность — нет трекинг-файла, backend `GET /api/v1/documents` шипнут вне story-lifecycle | Владельцу story 5: решить, 🔧 это или ещё «—», после бутстрапа `progress-backend.md` |
| Story 7 `resend-code` | Роут документирован, не реализован. Помечен «Planned» | Реализовать в рамках сценария resend или убрать из `endpoints.md` |
| Acceptance-тест для owner-scoped history | `GET /api/v1/generations`/`documents` покрыты unit/adapter/DB, но не acceptance-тестом | `tasks/done/4-bug-generations-auth/progress.md` называет тот же гэп |

## Низкий приоритет

- **Landing acceptance `hero-subheading` (#12 в реестре, помечен CLOSED 2026-07-10)** —
  тест ссылается на удалённый testid; депрайоритизировано пользователем. Всплывёт при
  следующем касании scenario 1.1.
- **`infra/Makefile`** — только Terraform-цели при dormant Terraform; шаблонный балласт.
- **`infra/.claude/` generic-агенты** (android/ios/flutter/terraform-yandex) — не по стеку,
  задел/балласт. Решение владельца: чистить или оставить.
