# Ре-аудит документации Textery — 20.07.2026

Продолжение аудита `00-05_*.md` (13.07.2026). Тот аудит закрыт: `CLEANUP_PLAN.md` — все
7 фаз `[x]`, поэтому большинство расхождений «документация vs код» уже устранены. Этот
проход проверяет состояние на 20.07.2026 (добавились Story 5, Story 7, Tasks 4, 5) и
фиксирует, что было исправлено сейчас.

## Главный вывод

**Документация в хорошем состоянии.** Крупный дрейф из аудита 13.07 (провайдер
OpenRouter→GigaChat, arq→BackgroundTasks, MSW→vi.mock, `infrastructure/`→`infra/`,
qodana-черновик, дубль CI, перенос Task 1/2 в done) — **всё закрыто** ещё в рамках
`CLEANUP_PLAN`. Проверено прямым чтением `technology.md`, `infra/.memory-bank/*`,
`.claude/skills/cleanup-chrome`, кода провайдера. Оставшийся дрейф — мелкий и локальный,
в основном в новых историях 5/7, которые тем аудитом не покрывались.

## Что проверено в этом проходе

| Область | Результат |
|---|---|
| `technology.md` (провайдер/очередь/mocking/линтер) | ✅ уже актуально (GigaChat, BackgroundTasks+sweep, vi.mock, oxlint) |
| `BriefProductDescription.md`, `ExpectedLoad.md` | ✅ провайдер уже GigaChat |
| `infra/.memory-bank/{index,open-questions}.md` | ✅ провайдер и CI уже актуализированы |
| `qodana.yaml` (черновик) | ✅ удалён ранее |
| `/cleanup-chrome` — запрет kill по имени exe | ✅ противоречие устранено ранее |
| Дубль CI (`.github` vs `backend/.github`) | ✅ не дубль — два репо (GitHub + GitVerse subtree), задокументировано в `infrastructure-detail.md` |
| Пустой каталог `infrastructure/` + `backend_run.log` в корне | 🔧 удалены сейчас (untracked, `*.log` в `.gitignore`) |
| Story 5 — `progress-backend.md` обещан, но отсутствует | 🔧 добавлена честная пометка в `progress.md` |
| Story 7 — `endpoints.md` `resend-code` (не реализован) | 🔧 помечен «Planned, not yet implemented» |
| Story 7 — нарратив «What backend does today» (2026-07-16) | 🔧 3 ложных утверждения исправлены (см. ниже) |
| known-debt #3/#5 — остаточные упоминания OpenRouter | 🔧 исправлены на GigaChat (флаг из #11) |

## Что исправлено сейчас (правки в этой сессии)

1. **Уборка мусора рабочей копии** — удалены `backend_run.log` (49 КБ) и пустой каталог
   `infrastructure/agent-progress.log`. Оба были untracked и под `*.log` в `.gitignore` —
   на git-историю не влияют, чистка чисто локальная. Реальная инфраструктура живёт в
   `infra/`, каталог `infrastructure/` — рудимент, окончательно убран.

2. **`.memory-bank/tasks/known-debt.md`** — в записях #3 и #5 оставались упоминания
   `OPENROUTER_API_KEY` и «OpenRouter call inside the arq worker». Запись #11 ещё
   13.07 пометила это как долг «поправить, когда будет время на doc-churn» — поправлено:
   секрет теперь `GIGACHAT_CREDENTIALS`/`GIGACHAT_CA_BUNDLE`, вызов — GigaChat через
   `BackgroundTasks`, с перекрёстными ссылками на #11/#13.

3. **Story 7 `progress.md`, нарратив «What the backend actually does today»** —
   датирован 2026-07-16, содержал 3 утверждения, ложных на 20.07 (проверено по коду):
   - «Verify не подключён в `main.py`, намеренно» — на деле `main.py:114` подключает
     `get_verify_account_usecase`, роут живой (`auth_router.py:47`).
   - Login/refresh не упомянуты — на деле подключены (`main.py:115-116`), роуты живые.
   - «Пароли хранятся в plaintext, хеширования нигде нет, это highest-priority» —
     на деле есть `BcryptPasswordHasher`, `PasswordHasher` — обязательный аргумент
     конструктора, `register_user.py:83` хеширует, `login_user.py:33` проверяет.

     Нарратив переписан по факту. Особо важно: старая формулировка про plaintext —
     активная дезинформация о безопасности (называла закрытую дыру «критической открытой»).

4. **Story 7 `endpoints.md`** — `POST /api/v1/auth/resend-code` документирован, но не
   реализован (`grep -rni resend backend` — 0). Помечен «Planned, not yet implemented»;
   примечание про возврат кода уточнено.

5. **Story 5 `progress.md`** — файл обещает `progress-backend.md` с чеклистом
   backend/integration/security-сценариев, но файл не создан. Добавлена честная пометка
   (по образцу story 7 про «progress-frontend.md, not yet created»): backend-трекинг ещё
   не забутстрапен, а маркеры `Back`/`Sec` = 🔧 в `stories.md` опережают реальное состояние.

## Что НЕ трогалось (сознательно)

- **`_project_audit/00-05` + `CLEANUP_PLAN.md`** — исторический артефакт. Добавлен только
  баннер в `00_SUMMARY.md`, что это снимок на 13.07 и есть свежий ре-аудит.
- **Маркеры `stories.md` для Story 5 (`Back`/`Sec` = 🔧)** — не переключал в «—»/«·»:
  часть backend (`GET /api/v1/documents`) реально шипнута, но вне story-lifecycle
  (см. заметку про #12 в `stories.md`). Флип маркеров без разбора рискует исказить —
  вынесено в техдолг как вопрос к владельцу истории.
- **`stories/01-.../02_UI_Tests.md` секция 4 + mockup 04** — намеренно устаревшие
  (known-debt #9), не источник истины. Не «чинил задним числом».
- **`.memory-bank/` (корневой)** — заморожен как исторический вход, кроме `known-debt.md`.

## Открытый техдолг

Вынесен отдельно — `TECH_DEBT_2026-07-20.md`. Канонический реестр остаётся
`.memory-bank/tasks/known-debt.md`; отдельный файл — срез актуально-открытого на 20.07.
