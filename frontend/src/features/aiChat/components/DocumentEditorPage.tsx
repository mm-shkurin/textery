// STUB — renders nothing (TDD red, Story 19 Frontend Scenario 0.1).
//
// The route `/documents/:documentId` does not exist in `app/App.tsx` yet either; wiring it, and
// filling this component with the not-found blocker / editor branch, is `green-frontend`.
export function DocumentEditorPage() {
  return null
}
