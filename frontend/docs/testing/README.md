# Тест-кейсы интерфейса

UI-сюиты десяти историй — то, что проверяется в браузере: экраны, состояния,
клавиатура, доступность, поведение при ошибке сети. Это **проектные тест-кейсы**,
а не отчёт автотестов: они пишутся до кода (`/test-spec`), из них растёт TDD-цикл,
и по ним же проверяется готовая история.

Автотесты живут отдельно и в другом масштабе: 960 тестов в `src/**/__tests__`
плюс Selenium-сценарии в `acceptance/tests/frontend/` монорепозитория. Кейс здесь
описывает, **что** должно работать; тест — как это проверено.

## Как читать кейс

Каждый кейс — заголовок `### TC-<история>-<сюита>-<номер> — что проверяется` и
таблица из шести полей:

| Поле | Что в нём |
|------|-----------|
| Description | зачем кейс существует — какую поломку он ловит |
| Preconditions | состояние системы до первого шага, с названным аккаунтом и фикстурой |
| Test data | конкретные значения, которые проверяющий вводит: id, тела запросов, пороги |
| Steps | пронумерованные действия, по одному действию на шаг |
| Expected result | точный наблюдаемый результат: код ответа, тело, заголовок, строка лога |
| Status | `Not run` / `Passed` / `Failed` — по нему прогон оставляет след |

Кейс называет то, что видно на экране: русский текст, `data-testid` контрола,
состояние кнопки. Источник — сами компоненты в `src/features/**`. Общая
таблица тестовых данных в начале каждого файла задаёт аккаунты и идентификаторы,
которые кейсы этого файла переиспользуют, чтобы каждый не заводил свои.

## Что где лежит

| Каталог | История |
|---------|---------|
| `01-auto-generate-doklad` | автогенерация доклада — основной путь продукта |
| `04-auto-generate-referat` | реферат: свой промпт и запрет выдуманных источников |
| `05-manual-mode` | ручной режим и редактор |
| `07-authorization` | регистрация, подтверждение кода, вход, сессия |
| `10-editor-pages` | страницы документа в редакторе |
| `12-my-projects` | «Мои проекты»: лента, поиск, сортировка, пагинация |
| `13-profile-management` | профиль: имя, аватар, тема, удаление аккаунта |
| `16-oauth-signin` | вход через VK и Яндекс |
| `17-export-document` | экспорт документа |
| `18-generate-then-edit` | генерация с последующим редактированием |

Внутри истории: `02_UI_Tests.md` — основная сюита, `extended/` — дополнительные
кейсы, если у истории они есть.

## Источник правды

Эти файлы — **копии**. Оригиналы лежат в монорепозитории, в
`ProductSpecification/stories/*/tests/`, и правятся там: у каждого файла здесь в
шапке написан его источник.

```bash
node scripts/sync-test-cases.mjs           # обновить копии
node scripts/sync-test-cases.mjs --check   # упасть, если копии устарели
```

Первую команду нужно выполнять **перед каждым релизным пушем** — иначе
опубликованные кейсы отстанут от тех, которые команда действительно ведёт.
Бэкенд публикует свои пять сюит тем же способом
(`backend/scripts/sync_test_cases.py`).
